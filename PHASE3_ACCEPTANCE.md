# 阶段 3 验收记录

> 记录日期：2026-08-16
> 范围：淘宝联盟真实商品搜索适配器、阶段 3 本地加固、Supabase Edge Function 部署、真实接口与本地真实模式浏览器验收；不包含知乎、转链、正式愿望持久化或 Pages 部署。

## 代码完成

- `products-search` 保持升级版 `taobao.tbk.dg.material.optional.upgrade`、27939 物料搜索与精确 CORS/JWT/POST/OPTIONS 边界。
- 淘宝原始字段通过纯函数映射为稳定 `ProductCard`；只返回必需候选字段，不返回佣金、PID、签名参数、密钥或淘宝原始响应。
- `coupon_share_url` 优先、`click_url` 后备；只接受 HTTP/HTTPS 官方推广链接，`//` 规范化为 `https://`；缺少必填字段的候选直接过滤。
- 商品服务日志只记录事件、成功/失败类别、结果数量和耗时；不记录搜索词、链接、JWT、PID、密钥或原始响应。
- 前端新增独立 `products-service`：真实模式建立/复用匿名 Supabase 会话后调用 `products-search`；不在 `main.js` 解析淘联原始嵌套字段。
- fixture 仅在本地开发 `?fixture=1` 显式模式启用，持续显示“开发测试数据”；真实模式和生产构建绝不回退 fixture。
- 新增仅限开发构建的 `?productTest=success|empty|timeout|permission|config|invalid|image` 本地测试情景；它与 `?fixture=1` 分离，明确标识“开发测试情景”，不调用 Supabase、淘宝、知乎或转链接口，生产构建不可用。
- 真实搜索加载文案为“正在搜索符合条件的商品”；真实错误页只显示白名单错误码映射的用户友好信息，不显示“开发测试”或原始远程错误。
- 价格仅在合法且不同的预估到手价存在时同时显示销售价和预估到手价；相同、空或非法预估价只显示一次销售价，并继续声明“实际价格以淘宝结算页为准”。凑单参考价仅在条件完整时显示。
- 淘宝外部字段不再插入 HTML 属性：图片、标题、价格、凑单条件和选择索引由 DOM API 写入；推广链接不进入页面；客户端和服务端均用 URL 解析限制 HTTP/HTTPS，服务端返回规范化 `href`。图片失败改为安全占位。
- `products-service` 已覆盖 `FunctionsHttpError.context` 的合法 JSON、非 JSON、无响应体和网络错误；仅接收本项目白名单错误码，其余统一为 `request_failed`。
- 已新增 `PHASE3_DEPLOY_MANIFEST.md`，列出 `products-search` 部署入口、完整本地依赖、`verify_jwt=true` 和既有 Secret 名称边界；不包含任何值。
- 选择真实商品后停在“商品已选定；知乎证据将在阶段 4 接入。”边界页，不展示 fixture 知乎证据、不启动倒计时、不点击推广链接。

## 静态检查

| 命令 | 结果 |
|---|---|
| `npm test` | 通过，24/24 |
| `npm run check` | 通过（源码密钥扫描、阶段 1/2/3 静态检查、构建、构建产物密钥扫描） |
| `npm run build` | 通过 |

## 外部接口验证

**通过。** 独立验收方使用已连接的 Supabase 插件，将 `products-search` 部署为远程活动版本 5；部署包含入口文件和两个共享依赖，`verify_jwt=true`。未安装 Supabase CLI，未修改 Auth、数据库或 RLS。

受控验收共使用 4 次机会：前两次分别从 `127.0.0.1:4173` 和 `localhost:4173` 发起，均在淘宝调用前被既有 `ALLOWED_ORIGINS` 以预检 403 拒绝，用于定位配置问题；第三次从已授权的 GitHub Pages 来源值通过匿名会话直接调用，成功返回 5 条真实淘宝候选；用户补充两个本地来源后，第四次在本地浏览器真实模式成功返回 5 条候选。候选均具备商品 ID、标题、价格、价格标签和合法官方推广链接。验收过程未输出、保存或记录完整推广链接、JWT、PID、App Secret 或淘宝原始响应。

## 浏览器验证（本地 Vite）

| 项目 | 结果 | 说明 |
|---|---|---|
| 显式 fixture 回归 | 通过 | `?fixture=1` 持续标识开发测试数据；商品选择仍进入 fixture 证据流程，仅用于阶段 2 回归 |
| `productTest=success` | 通过 | 展示开发测试情景；选择候选后停在“知乎证据将在阶段 4 接入”边界页 |
| `productTest=empty` | 通过 | 显示空结果，不自动补充候选 |
| `productTest=timeout/permission/config/invalid` | 通过 | 均显示用户友好错误，不显示原始响应，控制台错误 0 |
| `productTest=image` | 通过 | 图片 404 后显示安全“图片加载失败”占位，控制台错误 0 |
| 搜索中进入我的愿望 | 通过 | 旧搜索回调未覆盖列表，控制台错误 0 |
| 刷新恢复与绝对倒计时 | 通过 | fixture 封存后刷新仍显示同一绝对 `expiresAt` 倒计时，控制台错误 0 |
| 宽度 | 通过 | 320、375、390、430 与桌面 1024px 无横向溢出；这是桌面浏览器尺寸验证，不等同于真实 WebView |
| 生产构建禁用测试情景 | 通过 | `vite preview` 的 `?productTest=success` 不显示开发测试情景，而在未配置真实服务时显示配置提示，控制台错误 0 |
| 未配置真实模式 | 通过 | 不带 `?fixture=1` 时显示“商品服务尚未配置”，不展示 fixture，控制台错误 0 |
| 真实候选/选定边界页 | 通过 | 本地真实模式返回 5 条淘宝候选；选择第一条后保留同一商品及价格，停在“知乎证据将在阶段 4 接入”边界页，未外跳、未启动倒计时，控制台错误 0 |
| 真实 CORS/JWT 链路 | 通过 | 补充两个本地验收来源后，浏览器预检通过；匿名会话、JWT 和远程函数调用成功，网关记录 POST 200 |

## 未完成项和已知限制

- 未调用知乎接口、未启用或修改 `taoke-convert`、未接入正式愿望持久化、未进入阶段 4。
- 未部署 GitHub Pages、未推送或合并 `main`。
- 本阶段真实浏览器证据来自本地桌面受控浏览器；不冒充微信或知乎真实 WebView。目标 WebView 中的完整业务流程留待后续集成阶段验收。

## 当前结论

**阶段 3 已完成并冻结。** 冻结代码基线为 `8908331`；远程 `products-search` 活动版本为 5。代码、静态检查、真实淘宝接口、匿名会话、CORS/JWT 和本地真实模式浏览器候选选择均已分别通过。未推送、未合并、未部署 GitHub Pages，阶段 4 未开始。
