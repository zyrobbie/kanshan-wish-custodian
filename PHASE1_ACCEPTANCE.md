# 阶段 1：部署与身份持久化开工验证

更新时间：2026-08-12

本报告严格区分“代码存在”与“真实服务已验收”。未通过的关键项不会被标为完成。

## 当前状态

| 验收项 | 代码完成 | 静态检查 | 接口验证 | 浏览器验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| 凭据基线与忽略规则 | 是 | 通过（源码与构建产物扫描） | 不适用 | 不适用 | 本地通过 |
| Vite 最小诊断页 | 是 | 通过（Vite build） | 未配置 Supabase | 通过：未配置时禁用写入、无控制台错误 | 本地通过，远程阻塞 |
| wishes 表、外键、约束、索引、强制 RLS | 是（迁移文件） | 通过（控制项检查） | 通过：已应用，安全顾问无告警 | 尚未做两用户越权实测 | 部分通过 |
| 匿名登录、测试愿望、刷新恢复 | 是（诊断页） | 通过 | 通过：真实匿名用户与愿望写入 | 通过：Pages 刷新、关闭后重新打开仍读取 1 条愿望 | 通过 |
| 匿名用户绑定新邮箱且 uid 不变 | 未实现为可操作页面 | 不适用 | 需要 OTP 邮件配置 | 未验证 | 阻塞 |
| 既有邮箱 OTP 登录且禁止自动注册 | 未实现为可操作页面 | 不适用 | 需要 OTP 邮件配置 | 未验证 | 阻塞 |
| 跨浏览器匿名愿望原子迁移 | 是（双 JWT 验证与受控 Edge Function） | 通过（控制项检查） | 未部署 | 未验证 | 阻塞 |
| 两个普通用户会话 RLS 越权拒绝 | 是（策略） | 通过（控制项检查） | 未应用到项目 | 未验证 | 阻塞 |
| 淘宝、知乎、转链 Edge Function 冒烟 | 是（函数代码） | 通过（构建/扫描） | 未部署、Secrets 未配置 | 未验证 | 阻塞 |
| GitHub Actions / Pages | 是（工作流） | 通过（本地与 Actions 构建） | 通过：HTTPS Pages 已部署 | 通过：桌面 Chrome 访问诊断页 | 通过 |
| 重部署后数据保留 | 不适用 | 不适用 | 未部署 | 未验证 | 阻塞 |
| 微信/知乎真实 WebView | 不适用 | 不适用 | 未部署 | 未验证 | 等待可访问正式地址 |

## 外部阻塞项（一次性确认）

1. 邮件：正式 OTP 发信服务、已验证发信域名与发件人；需要由你接管登录或完成域名/SMTP 验证，切勿在对话中发送密钥。
2. 允许将淘宝、知乎、转链凭据仅写入对应 Supabase 项目 Secrets；目前本机知乎 Skill 未配置 Access Secret，不能进行知乎真实冒烟。

## 已知安全说明

- GitHub 仓库、HTTPS Pages 与 Supabase 新加坡项目已创建；正式 Pages 地址为 `https://zyrobbie.github.io/kanshan-wish-custodian/`，并已设为 Supabase Auth Site URL。
- `.env.taobao.local` 未读取，权限检查为 `600`；新增扫描会跳过所有本地 `.env*`，仅扫描可提交源文件与 `.env.example`。
- Edge Functions 只返回已归一化的业务字段；日志只记录事件、成功状态和数量，不输出密钥、完整邮箱或供应商响应体。
- 匿名迁移要求目标邮箱会话放在 `Authorization`、源匿名会话放在 `X-Source-Authorization`；函数先验证两枚 JWT，再以只在函数环境中可用的 service-role Secret 执行单条原子更新。浏览器无法指定目标 `owner_id`，普通用户愿望读写仍由 RLS 保护。

## 已验证证据

- 2026-08-13：Pages Actions `31670178156` 成功，HTTPS 已启用。
- 2026-08-13：匿名用户已创建并写入 1 条测试愿望；刷新及关闭后重开后仍读取到同一条记录。此处只记录结果计数，不记录邮箱、用户 ID 或愿望 ID。
