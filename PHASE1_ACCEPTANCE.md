# 阶段 1：部署与身份持久化开工验证

更新时间：2026-08-14

本报告严格区分“代码存在”与“真实服务已验收”。

## 阶段结论

阶段 1 已完成：数据保留、微信真实 WebView、知乎真实 WebView 三项最后验收均通过。受控浏览器的淘宝链接点击未离开 Pages 已单独保留为环境差异；普通桌面 Chrome 的人工落地核对已通过，不影响该项的真实浏览器验收结论。

## 当前状态

| 验收项 | 代码完成 | 静态检查 | 接口验证 | 浏览器验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| 凭据基线与忽略规则 | 是 | 通过（源码与构建产物扫描） | 不适用 | 不适用 | 本地通过 |
| Vite 最小诊断页 | 是 | 通过（Vite build） | 通过：公开配置加载、真实 Auth/REST 调用 | 通过：桌面 Pages 可用 | 通过 |
| wishes 表、外键、约束、索引、强制 RLS | 是（迁移文件） | 通过（控制项检查） | 通过：已应用，安全顾问无告警 | 通过：两普通用户会话越权均返回 0 | 通过 |
| 匿名登录、测试愿望、刷新恢复 | 是（诊断页） | 通过 | 通过：真实匿名用户与愿望写入 | 通过：Pages 刷新、关闭后重新打开仍读取 1 条愿望 | 通过 |
| 匿名用户绑定新邮箱且 uid 不变 | 是（诊断页） | 通过 | 通过：Supabase Auth Email Change OTP | 通过：QQ 与 Gmail 均验证 uid 不变、原愿望保留 | 通过 |
| 既有邮箱 OTP 登录且禁止自动注册 | 是（诊断页） | 通过：`shouldCreateUser: false` | 通过：不存在 Gmail 返回 `Signups not allowed for otp`，不发送邮件 | 通过：新浏览器 QQ OTP 登录成功并恢复 3 条既有愿望 | 通过 |
| 跨浏览器匿名愿望原子迁移 | 是（双 JWT 验证与受控 Edge Function） | 通过（控制项检查） | 通过：函数已部署，JWT 与 Pages CORS 均启用 | 通过：迁入 1 条；刷新后总数保持 7，不重复复制 | 通过 |
| 两个普通用户会话 RLS 越权拒绝 | 是（策略与诊断自检） | 通过（控制项检查） | 通过：两枚普通匿名 JWT 访问同一 Data API | 通过：B 对 A 读取、修改、删除均返回 0；A 记录未变并已清理 | 通过 |
| 淘宝搜索与官方推广链接 | 是（受 JWT 保护的函数与真实 `<a>` 诊断链接） | 通过（构建/扫描；无脚本跳转） | 通过：普通匿名用户会话真实调用一次，返回 5 条同时具备必填字段与官方推广链接的候选 | 受控浏览器未通过；普通桌面 Chrome 手动核对通过：当前标签进入淘宝商品页，商品与候选一致 | 通过 |
| 知乎搜索 Edge Function 冒烟 | 是（受 JWT 保护的函数与最小诊断按钮） | 通过（构建/扫描） | 通过：普通匿名用户会话真实调用一次，返回 5 条规范化结果 | 通过：HTTPS Pages 日志未记录内容链接或凭据 | 通过 |
| 淘客转链 Edge Function | 源码保留，Demo 已停用 | 不适用 | 不再调用 | 不适用 | 已移出阶段 1 门槛 |
| GitHub Actions / Pages | 是（工作流） | 通过（本地与 Actions 构建） | 通过：HTTPS Pages 已部署 | 通过：桌面 Chrome 访问诊断页 | 通过 |
| 重部署后数据保留 | 不适用 | 不适用 | 提交 `a166e02` 的 Pages 重部署成功 | 通过：绑定邮箱 OTP 登录后读取到部署前既有记录 | 通过 |
| 微信/知乎真实 WebView | 不适用 | 不适用 | 通过：真实手机微信与知乎 App 均完成 Pages 加载、会话和愿望读取请求 | 通过：无明显横向溢出、按钮可点；切出 App 再返回后仍可使用 | 通过 |

## 已知环境差异

受控浏览器的官方推广链接点击未离开 Pages；普通桌面 Chrome 已完成一次人工核对，当前标签进入淘宝商品页且商品与候选一致。两种环境结果分别记录，不将受控浏览器失败掩盖为通过。

## 已知安全说明

- GitHub 仓库、HTTPS Pages 与 Supabase 新加坡项目已创建；正式 Pages 地址为 `https://zyrobbie.github.io/kanshan-wish-custodian/`，并已设为 Supabase Auth Site URL。
- Resend 已验证 `auth.kanshanwish.tech`；Supabase Auth 已使用 Resend SMTP。发送方为 `no-reply@auth.kanshanwish.tech`；SMTP/API 凭据未写入仓库、Pages 或本报告。
- `.env.taobao.local` 未读取，权限检查为 `600`；新增扫描会跳过所有本地 `.env*`，仅扫描可提交源文件与 `.env.example`。
- Edge Functions 只返回已归一化的业务字段；日志只记录事件、成功状态和数量，不输出密钥、完整邮箱或供应商响应体。
- 匿名迁移要求目标邮箱会话放在 `Authorization`、源匿名会话放在 `X-Source-Authorization`；函数先验证两枚 JWT，再以只在函数环境中可用的默认 service-role 环境变量执行单条原子更新。浏览器无法指定目标 `owner_id`，普通用户愿望读写仍由 RLS 保护。

## 已验证证据

- 2026-08-13：Pages Actions `31670178156` 成功，HTTPS 已启用。
- 2026-08-13：匿名用户已创建并写入 1 条测试愿望；刷新及关闭后重开后仍读取到同一条记录。此处只记录结果计数，不记录邮箱、用户 ID 或愿望 ID。
- 2026-08-13：QQ 邮箱：匿名绑定、跨浏览器 OTP 登录均即时送达至正常收件箱；绑定后 uid 不变，跨浏览器登录后读取 3 条既有愿望。
- 2026-08-13：Gmail：匿名绑定验证码在 1 分钟内送达正常收件箱；写入 1 条愿望后绑定，uid 不变且仍读取 1 条。
- 2026-08-13：不存在的 Gmail 使用已有账户 OTP 登录返回 `Signups not allowed for otp`，未自动注册；匿名愿望迁移函数迁入 1 条，刷新后愿望总数保持 7。
- 2026-08-14：两个独立普通匿名用户会话完成 RLS 越权验证；用户 B 对用户 A 的指定愿望读取、修改、删除均返回 0，A 的愿望状态未变且由 A 清理。
- 2026-08-14：淘宝搜索 Edge Function 已部署并保留 JWT 校验；一次普通匿名会话从 HTTPS Pages 发起 `手机壳` 查询，成功返回 5 条规范化结果。页面与函数日志均未记录商品链接、会话令牌或联盟凭据。
- 2026-08-14：知乎搜索 Edge Function 已部署并保留 JWT 校验；一次普通匿名会话从 HTTPS Pages 发起 `手机壳` 查询，成功返回 5 条规范化结果。页面与函数日志均未记录内容链接、会话令牌或知乎凭据。
- 2026-08-14：淘客转链 Edge Function 已部署并保留 JWT 校验；使用诊断素材真实调用一次，Edge Functions 访问日志记录 `OPTIONS 204` 与 `POST 502`，总耗时约 2 秒。响应、页面日志和本报告均未显示推广链接、Access Key、PID 或供应商原始响应；未自动重试。
- 2026-08-14：用户提供一条淘宝短链后，普通匿名用户会话从 HTTPS Pages 再做一次受控复验，仍得到 `POST 502`。未展示或记录商品链接、推广链接、Access Key、PID 或供应商原始响应；当前诊断页已改为临时输入素材，不再在其源码中保存该短链。
- 2026-08-14：在账号与 PID 一致确认后，转链函数部署脱敏故障分类版本并完成最后一次受控调用；HTTPS Pages 显示 `provider_conversion_rejected`。这证明联盟 MCP 初始化已接受该 Key，实际转链结果未成功；未输出推广链接、PID、Access Key 或供应商原始错误。
- 2026-08-14：用户确认该商品的最简链接在淘宝联盟人工万能转链中亦提示“不支持转化”。该商品不再用于验收；已冻结产品边界：用户可搜索任意名称，但最终候选仅来自淘宝联盟可推广商品池，不承诺任意指定淘宝商品都可转链。
- 2026-08-14：完成唯一营销 ID 对照验收：普通匿名会话先由升级版物料搜索取得 5 件候选，再仅传其中一个营销 `item_id` 给转链函数。结果仍为 `provider_conversion_rejected`（函数访问日志：正常预检后 `POST 502`）。未显示或记录推广链接、PID、Access Key、商品链接或供应商原始响应；不再重试。
- 2026-08-14：Demo 已停用淘客转链入口，`taoke-convert` 源码保留但已移出阶段 1 验收门槛。`products-search` 只读取 `item.publish_info.coupon_share_url`，并以 `item.publish_info.click_url` 后备；仅保留具有有效官方链接的候选，页面与函数日志不记录推广链接、App Secret 或 PID。
- 2026-08-14：一次普通匿名会话真实搜索返回 5 条带官方推广链接的候选。唯一一次页面“官方推广链接落地”动作未使受控浏览器离开诊断页；未再次搜索、点击或调用 `taoke-convert`，故浏览器落地验收如实记录为未通过。
- 2026-08-14：提交 `6e06fe8` 的 Pages 部署工作流 `31778359484` 成功。诊断页将“验证首个官方推广链接落地”改为真实 HTML `<a href>`，搜索成功后仅显示目标域名和候选标题；页面和日志均不显示完整 `promotionUrl`，且源码不再使用 `window.location.assign`。
- 2026-08-14：部署后按新限额进行一次普通匿名会话搜索，返回 5 条带有效官方推广链接的候选；页面只显示目标域名 `s.click.taobao.com`。随后仅点击一次真实 `<a>`；受控浏览器仍停留在 GitHub Pages，未进入淘宝/Tmall。由于没有离开诊断页，不能核对落地商品是否与候选一致；没有进行第二次搜索、点击或转链调用。
- 2026-08-14：普通桌面 Chrome 人工验收：匿名会话建立后完成一次搜索，再点击真实 `<a>`；当前标签进入淘宝商品页，落地商品与页面候选一致。完整推广链接及其跟踪参数未写入页面、日志、仓库或本报告。
- 2026-08-14：提交 `a166e02` 的 Pages 重部署完成后，既有绑定邮箱通过 OTP 登录并读取愿望；既有记录存在=true、总数=7。未记录邮箱、UID、愿望 ID 或愿望内容。
- 2026-08-14：真实手机微信 App WebView 验收通过：Pages 正常加载，无明显横向溢出，按钮可点击；会话创建或恢复与愿望读取请求成功，切出 App 再返回后页面仍可使用。读取总数=7；未记录邮箱、UID、愿望 ID 或内容。
- 2026-08-14：真实手机知乎 App WebView 验收通过：Pages 正常加载，无明显横向溢出，按钮可点击；会话创建或恢复与愿望读取请求成功，切出 App 再返回后页面仍可使用。读取总数=7；未记录邮箱、UID、愿望 ID 或内容。
