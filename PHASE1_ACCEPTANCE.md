# 阶段 1：部署与身份持久化开工验证

更新时间：2026-08-12

本报告严格区分“代码存在”与“真实服务已验收”。未通过的关键项不会被标为完成。

## 当前状态

| 验收项 | 代码完成 | 静态检查 | 接口验证 | 浏览器验证 | 状态 |
| --- | --- | --- | --- | --- | --- |
| 凭据基线与忽略规则 | 是 | 通过（源码与构建产物扫描） | 不适用 | 不适用 | 本地通过 |
| Vite 最小诊断页 | 是 | 通过（Vite build） | 未配置 Supabase | 通过：未配置时禁用写入、无控制台错误 | 本地通过，远程阻塞 |
| wishes 表、外键、约束、索引、强制 RLS | 是（迁移文件） | 通过（控制项检查） | 未应用到项目 | 未验证 | 阻塞 |
| 匿名登录、测试愿望、刷新恢复 | 是（诊断页） | 通过 | 未配置 Supabase | 未验证 | 阻塞 |
| 匿名用户绑定新邮箱且 uid 不变 | 未实现为可操作页面 | 不适用 | 需要 OTP 邮件配置 | 未验证 | 阻塞 |
| 既有邮箱 OTP 登录且禁止自动注册 | 未实现为可操作页面 | 不适用 | 需要 OTP 邮件配置 | 未验证 | 阻塞 |
| 跨浏览器匿名愿望原子迁移 | 是（SQL/RPC 与 Edge Function） | 通过（控制项检查） | 未部署 | 未验证 | 阻塞 |
| 两个普通用户会话 RLS 越权拒绝 | 是（策略） | 通过（控制项检查） | 未应用到项目 | 未验证 | 阻塞 |
| 淘宝、知乎、转链 Edge Function 冒烟 | 是（函数代码） | 通过（构建/扫描） | 未部署、Secrets 未配置 | 未验证 | 阻塞 |
| GitHub Actions / Pages | 是（工作流骨架） | 通过（本地构建使用同一命令） | 无 remote、未部署 | 未验证 | 阻塞 |
| 重部署后数据保留 | 不适用 | 不适用 | 未部署 | 未验证 | 阻塞 |
| 微信/知乎真实 WebView | 不适用 | 不适用 | 未部署 | 未验证 | 等待可访问正式地址 |

## 外部阻塞项（一次性确认）

1. GitHub：目标账号/组织与新仓库名；是否授权我在你恢复 GitHub 登录后创建该仓库、添加 remote、提交、推送并启用 Pages。
2. Supabase：目标账号/组织、项目名与部署区域；是否授权创建项目并安装/使用 Supabase CLI。
3. 邮件：正式 OTP 发信服务、已验证发信域名与发件人；需要由你接管登录或完成域名/SMTP 验证，切勿在对话中发送密钥。
4. 正式 Pages 地址（项目页路径或已拥有的自定义域名）与允许的本地开发来源；该值决定 Vite `base`、Auth Site URL、Redirect URLs、CORS `ALLOWED_ORIGINS`。
5. 允许将淘宝、知乎、转链凭据仅写入对应 Supabase 项目 Secrets；目前本机知乎 Skill 未配置 Access Secret，不能进行知乎真实冒烟。

## 已知安全说明

- 当前仓库没有 remote；不会创建远程资源、提交或推送。
- `.env.taobao.local` 未读取，权限检查为 `600`；新增扫描会跳过所有本地 `.env*`，仅扫描可提交源文件与 `.env.example`。
- Edge Functions 只返回已归一化的业务字段；日志只记录事件、成功状态和数量，不输出密钥、完整邮箱或供应商响应体。
- 匿名迁移要求目标邮箱会话放在 `Authorization`、源匿名会话放在 `X-Source-Authorization`；函数先验证两枚 JWT，再以目标 JWT 调用受控 SQL 函数，避免浏览器指定目标 `owner_id`。
