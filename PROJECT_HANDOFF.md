# 看山·愿望保管员 Demo 项目交接手册

> 面向第一次接触本项目、并将长期承担开发、部署、测试和验收工作的团队。

## 0. 交接基线

| 项目 | 当前值 |
|---|---|
| 交接日期 | 2026-08-16 |
| 本地工作目录 | `/Users/zhihu/Documents/ChatGPT/看山愿望商店` |
| GitHub 仓库 | `https://github.com/zyrobbie/kanshan-wish-custodian` |
| 默认分支 | `main` |
| 阶段 1 验收提交 | `af3449ec26146bd42344df49165d06206ba4802d`（短提交号 `af3449e`） |
| 公网诊断地址 | `https://zyrobbie.github.io/kanshan-wish-custodian/` |
| 当前阶段 | 阶段 1–4 已完成；阶段 5 本地实现完成、远程验收待进行；阶段 6 未开始 |
| 当前交付物性质 | 阶段 1 在线诊断基线＋阶段 2 本地静态产品流程＋阶段 3 真实淘宝搜索＋阶段 4 真实知乎双层内容；尚不是正式愿望持久化的最终 Demo |
| 阶段 2 冻结代码基线 | `4c29383`（初始功能提交 `19998f7`） |
| 阶段 3 冻结代码基线 | `8908331`（远程 `products-search` 活动版本 5） |
| 阶段 4 冻结代码基线 | `07b6a4d`（远程 `zhihu-search` 活动版本 3） |

接手团队开始任何工作前，必须先确认 `main` 与 `origin/main` 仍在阶段 1 公网基线 `af3449e`，阶段 2 冻结实现位于 `codex/phase2-static-flow` 的 `4c29383`，阶段 4 冻结实现位于 `codex/phase4-zhihu-evidence` 的 `07b6a4d`，并确认工作区没有来源不明的未提交修改。

禁止为了“整理环境”执行 `git reset --hard`、批量删除未跟踪文件、重建 Supabase 项目或清空远端数据。远端目前存在真实验收用户和愿望记录；任何清理都必须先得到项目负责人明确授权。

---

## 1. 三分钟理解项目

### 1.1 项目是什么

“看山·愿望保管员”是用于知乎官方活动的可交互 Web Demo。用户输入想购买的商品，系统从淘宝联盟取得真实商品候选，再从知乎取得“专业解读”和“真实体验”两层可追溯内容。用户选择一件商品并交给看山保管，Demo 把 24/48/72 小时压缩为 24/48/72 秒；倒计时结束后，用户再决定“还想买”或“不要了”。

产品主张是：**把冲动交给看山，把决定留给自己。**

本项目不是电商平台，不在站内支付，不承诺替用户省下真实金额，也不替用户决定是否购买。

### 1.2 核心真实链路

```text
GitHub Pages 静态前端
  ↓ HTTPS
Supabase Auth（匿名用户、邮箱 OTP）
  ↓ 用户 JWT
Supabase Postgres + RLS（愿望持久化与用户隔离）
  ↓
Supabase Edge Functions
  ├─ 淘宝联盟升级版物料搜索
  ├─ 知乎开放平台搜索
  └─ 匿名愿望迁移
```

### 1.3 最重要的业务边界

1. 用户可以搜索任意合法商品名称，但候选只能来自淘宝联盟当次返回的可推广商品池。
2. 系统不承诺任意淘宝链接、商品 ID 或指定商品都能成为候选。
3. 商品购买链接直接使用淘宝联盟搜索响应中的官方 `promotionUrl`：优先 `publish_info.coupon_share_url`，缺失时使用 `publish_info.click_url`。
4. 当前 Demo 主链路不再调用转链 Skill；不得私自重新启用。
5. 淘宝候选按相关性展示，不按佣金率排序；前端不展示佣金率，不使用强营销文案。
6. 知乎内容必须保留标题、作者、来源和原文链接；不能用淘宝文案或固定样例冒充实时知乎结果。
7. “还想买”只代表用户选择前往淘宝，不代表成交。
8. “不要了”记录的是“这次放下的商品金额”，不是经过审计的真实财务节省。
9. 所有预估价格都必须提示“实际价格以淘宝结算页为准”。
10. 任何接口失败都必须诚实显示失败或降级状态，禁止以假数据冒充实时成功。

---

## 2. 文档清单与阅读顺序

### 2.1 必读文档

1. `PROJECT_HANDOFF.md`（本文件）
   - 新团队的入口、当前事实、开发和验收 SOP。
2. `PHASE1_ACCEPTANCE.md`
   - 阶段 1 的最新真实验收证据，是当前运行状态的最高优先级事实来源。
3. `PHASE2_ACCEPTANCE.md`
   - 阶段 2 的最新验收依据，记录静态流程、异步导航返工和独立浏览器复验；冻结代码基线为 `4c29383`。
4. `PHASE3_ACCEPTANCE.md`
   - 阶段 3 的最终验收依据；记录远程函数、真实淘宝接口和本地真实模式浏览器候选选择证据。
5. `PHASE4_ACCEPTANCE.md`
   - 阶段 4 本地实现、远程函数、三类商品真实双层接口和浏览器验收的最终依据。
6. `PROJECT_SPEC.md`
   - 产品范围、页面、状态机、数据模型、技术架构、测试方案、阶段路线和最终完成定义。
7. `.env.example`
   - 环境变量名称和安全边界；只允许示例值，不能写入真实凭据。
8. `.github/workflows/deploy-pages.yml`
   - `main` 分支自动构建和部署 GitHub Pages 的流程。
9. `package.json`
   - 本地启动、构建、静态控制检查和密钥扫描命令。

### 2.2 文档优先级

发生冲突时按以下顺序处理：

1. 项目负责人最新明确书面决定；
2. 最新阶段验收报告，例如 `PHASE1_ACCEPTANCE.md`、`PHASE2_ACCEPTANCE.md`；
3. 本交接手册；
4. `PROJECT_SPEC.md` 中与当前阶段一致的冻结产品决策；
5. 历史脚本、注释和旧诊断代码。

不能因为旧代码仍存在，就推断它仍属于产品主路径。

### 2.3 已完成的阶段 2 前文档修订记录

以下为阶段 2 开始前识别的历史文档债务。阶段 2 已完成并冻结；后续接手时可据此追溯，不得把这些历史措辞当作当前启动条件：

- 文档开头仍有“尚未创建前端、数据库或部署配置”等阶段 0 历史表述；实际阶段 1 已完成。
- 后端接口基线仍列出 `/api/links/convert`；当前 Demo 主路径不使用该接口。
- 端到端测试清单仍有“转链失败并走搜索推广链接降级”等旧措辞；应改为“官方推广链接缺失或落地失败”。
- “开发前仍需完成的外部配置”包含部分阶段 1 已完成项，也包含尚未真正实现的长期项，必须重新区分“已完成”“后续必须实现”和“上线前确认”。
- 文档中有重复的 `## 14`、`## 17` 标题，后续整理时只修结构，不改变已冻结产品含义。

任何后续修订仍必须先改文档、再改代码；不得反向用临时代码定义产品范围。

---

## 3. 当前真实状态

### 3.1 已完成：阶段 1

阶段 1 已在提交 `af3449e` 记录为完成。以下内容已有真实证据，详细过程见 `PHASE1_ACCEPTANCE.md`：

- Vite 最小诊断页和 GitHub Pages HTTPS 部署；
- Supabase 匿名登录；
- `wishes` 表、约束、索引、RLS 和强制 RLS；
- 匿名愿望写入、刷新、关闭后重开恢复；
- 匿名用户绑定新邮箱且用户 ID 不变；
- 已有邮箱 OTP 登录，且明确禁止登录入口自动注册未知邮箱；
- 新浏览器匿名愿望迁移到已验证邮箱账户；
- 两个普通用户会话之间的 RLS 越权读取、更新和删除拒绝；
- 淘宝联盟升级版搜索真实调用；
- 官方推广链接由真实 `<a href>` 在普通桌面 Chrome 当前标签页打开，商品与候选一致；
- 知乎开放平台真实搜索；
- GitHub Pages 重部署后仍可读取部署前的既有愿望；
- 微信和知乎真实 WebView 的 Pages 加载、会话、愿望读取、切出返回；
- 源码和构建产物密钥扫描。

### 3.2 已知环境差异，不应被掩盖

受控自动化浏览器点击淘宝官方推广链接时没有离开 GitHub Pages；普通桌面 Chrome 人工验收已成功进入正确淘宝商品。该差异已写入验收报告。

以后测试必须继续分别记录：

- 自动化浏览器结果；
- 普通桌面浏览器结果；
- 微信/知乎真实 WebView 结果。

不能用其中一种环境的结果替代其他环境。

### 3.3 明确尚未完成

以下内容没有因为阶段 1 完成而自动完成：

- 正式产品视觉和页面流程；
- 用户自由输入搜索词的正式商品候选页面；
- 知乎“专业解读＋真实体验”两层并行搜索、筛选和卡片；
- 24/48/72 秒倒计时、封存动画和到期恢复；
- 正式“还想买／不要了”决策事务；
- 放弃金额防重复累计；
- “我的愿望”正式时间线、分页、删除、清空和删除账户；
- 匿名用户 30 天清理 Cron；
- 公开接口限速、缓存、全站预算和熔断；
- 正式隐私说明与处理主体/联系方式；
- 完整手机尺寸矩阵、iOS/Android 双平台矩阵；
- 完整 WebView 淘宝外跳失败降级；
- 单元测试和正式端到端测试套件；
- 三次连续完整演示排练；
- 最终 Demo。

不得把阶段 1 诊断页包装成“产品已完成”。

---

## 4. 仓库结构与文件职责

```text
看山愿望商店/
├─ PROJECT_HANDOFF.md                  # 本交接手册
├─ PROJECT_SPEC.md                     # 产品和开发总规格
├─ PHASE1_ACCEPTANCE.md                # 阶段 1 验收证据
├─ index.html                          # 阶段 2 本地静态产品页；生产构建不提供 fixture
├─ src/
│  ├─ main.js                          # 当前诊断交互、Auth、RLS/接口冒烟入口
│  └─ style.css                        # 当前诊断页样式
├─ supabase/
│  ├─ migrations/
│  │  └─ 202608120001_phase1_wishes.sql
│  └─ functions/
│     ├─ _shared/http.ts               # 共享 CORS/JSON/脱敏错误工具
│     ├─ products-search/index.ts      # 淘宝联盟升级版搜索与字段归一化
│     ├─ zhihu-search/index.ts         # 知乎搜索与摘要清洗
│     ├─ migrate-anonymous-wishes/     # 双会话验证后的匿名愿望迁移
│     └─ taoke-convert/index.ts        # 历史转链适配器，源码保留但主路径停用
├─ scripts/
│  ├─ verify-phase1-static.mjs         # 阶段 1 控制项检查
│  ├─ secret-scan.mjs                  # 源码密钥扫描
│  ├─ scan-dist-secrets.mjs            # 构建产物密钥扫描
│  ├─ verify-taobao.mjs                # 淘宝接口独立诊断脚本
│  └─ verify-taobao-transfer.mjs       # 历史转链诊断脚本，不属于主路径
├─ .github/workflows/deploy-pages.yml  # main 自动部署 Pages
├─ .env.example                        # 变量名称模板，无真实值
├─ vite.config.js                      # GitHub Pages base 配置
├─ package.json
└─ package-lock.json
```

### 4.1 不能误用的历史文件

`supabase/functions/taoke-convert/index.ts` 和 `scripts/verify-taobao-transfer.mjs` 仅保留历史诊断价值：

- 不得接入正式页面；
- 不得加入自动重试；
- 不得作为官方推广链接失败时的降级；
- 不得把历史失败改写为已通过；
- 如未来确有业务理由重新评估，必须先单独立项、重新确认权限和验收标准。

---

## 5. 账号、权限和凭据交接

### 5.1 必须移交的外部访问权限

项目负责人应通过各平台正式成员/协作者机制给新团队授权，而不是共享个人密码：

1. GitHub 仓库协作权限；
2. GitHub Pages、Actions 和 Environment/Variables 查看维护权限；
3. Supabase 组织/项目访问权限；项目当前部署区域为新加坡；
4. Resend 项目或团队权限；
5. `kanshanwish.tech` DNS 管理权限或可执行 DNS 变更的联系人；
6. 淘宝开放平台应用访问权限；
7. 淘宝联盟账号、媒体和推广位的合规使用权限；
8. 知乎开放平台项目访问权限；
9. 活动主办方提供的任何正式入口、额度或桥接能力说明。

QQ/Gmail 等个人测试邮箱密码不得交接。新团队应建立团队自有测试邮箱，并继续验证 QQ 和 Gmail 等常见收件域。

### 5.2 GitHub 环境变量

GitHub Pages 工作流依赖以下公开构建变量：

- `VITE_BASE_PATH`
- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_PUBLISHABLE_KEY`

当前 Pages 地址使用项目子路径，`VITE_BASE_PATH` 应与 `/kanshan-wish-custodian/` 一致。修改仓库名、Pages 地址或自有域名时，必须同步检查 Vite base、Supabase Auth Site URL、Redirect URLs 和 CORS 来源。

### 5.3 Supabase Edge Functions Secrets

当前或历史函数涉及以下变量名：

- `ALLOWED_ORIGINS`
- `TAOBAO_APP_KEY`
- `TAOBAO_APP_SECRET`
- `TAOBAO_PID`
- `TAOBAO_ADZONE_ID`
- `ZHIHU_ACCESS_SECRET`
- `TAOKE_CONVERT_ACCESS_KEY`（历史适配器专用，当前主路径不需要）

Supabase 托管环境还提供项目 URL、公开/匿名 Key 和 service-role/secret Key。service-role 只允许在受控服务端函数内使用，绝不能进入浏览器。

交接文档、Issue、PR、聊天、截图和日志中只写变量名与配置状态，不写真实值。真实凭据通过密码管理器、平台成员权限或受控 Secrets 管理完成交接。

### 5.4 邮件配置

当前已知配置：

- 发信服务：Resend；
- 已验证子域名：`auth.kanshanwish.tech`；
- 发件人：`no-reply@auth.kanshanwish.tech`；
- Supabase Auth 已使用 Resend SMTP；
- QQ 和 Gmail 收件曾真实通过。

新团队必须在 Supabase Auth 和 Resend 控制台查看当前配置状态，不得从仓库寻找 SMTP 密码或 API Key。修改发信域名、发件人或 Supabase 项目后，应重新验证 SPF、DKIM、DMARC、OTP 模板和实际送达。

---

## 6. 本地开发和基线检查

### 6.1 环境要求

- macOS/Linux 均可；
- Node.js 22，与 GitHub Actions 保持一致；
- npm，使用已提交的 `package-lock.json`；
- Git；
- 需要部署 Edge Functions 时再配置 Supabase CLI，并先用 `supabase --help` 核对当前命令；
- 真实接口验证需要得到授权的网络和平台凭据。

### 6.2 首次拉取

```bash
git clone https://github.com/zyrobbie/kanshan-wish-custodian.git
cd kanshan-wish-custodian
git checkout main
git pull --ff-only
git rev-parse HEAD
npm ci
npm run check
```

预期基线提交至少包含 `af3449e`。`npm run check` 必须依次完成：

1. 源码密钥扫描；
2. 阶段 1 静态控制项检查；
3. Vite 构建；
4. 构建产物密钥扫描。

### 6.3 本地启动

1. 复制 `.env.example` 为不提交的本地环境文件；
2. 只在前端环境文件中填写浏览器公开设计的 Supabase URL 和 publishable key；
3. 淘宝、知乎、SMTP、service-role 等私密值不得放入 Vite 前端变量；
4. 启动：

```bash
npm run dev
```

5. 开发完成前再次运行：

```bash
npm run check
```

本地环境文件必须继续被 `.gitignore` 忽略。不得使用 `git add -f` 强行提交。

---

## 7. 部署流程和变更控制

### 7.1 GitHub Pages

`main` 每次 push 都会触发 `.github/workflows/deploy-pages.yml`，执行检查、构建并部署公网 Pages。因此：

- 不要把未验收的半成品直接推到 `main`；
- 使用功能分支和 PR；
- PR 合并前必须通过 `npm run check`；
- 合并后检查 GitHub Actions 成功；
- 部署后必须进行与改动风险相称的公网验证；
- 失败部署不得通过重跑掩盖根因，应先看构建日志和配置差异。

建议在项目负责人同意后，为 `af3449e` 创建只读阶段基线标签，例如 `phase1-accepted`，便于长期回归对比。不要未经授权自行创建或覆盖远端标签。

### 7.2 Supabase 变更

- 数据库结构变更必须以新的迁移文件保存，不直接修改已有阶段 1 迁移历史；
- 变更前确认 CLI 版本和命令帮助；
- 先在安全环境验证，再应用远端；
- 所有暴露给 Data API 的业务表必须启用 RLS；
- RLS 必须同时验证读取、插入、更新、删除和跨用户越权；
- service-role 只能用于明确受控的服务端操作；
- Edge Function 变更要复验 JWT、CORS、输入校验、超时、日志脱敏和错误响应；
- Auth、SMTP、Secrets、Redirect URLs 等控制台配置不在 Git 历史中，新团队必须维护独立的“配置状态清单”，但不得复制密钥值。

### 7.3 数据保护

- 远端已有验收数据，不得批量清理；
- 需要创建测试记录时使用明确测试标题，并在验收后由拥有者安全清理；
- RLS 测试必须使用普通用户会话，不得用 service-role 模拟普通用户；
- 任何删除、迁移、最终决定必须设计为幂等；
- 生产/演示数据和本地 fixture 必须明显区分。

---

## 8. 外部接口规范

### 8.1 淘宝联盟

当前唯一商品搜索主接口：

- 权限包：`27939`；
- API：`taobao.tbk.dg.material.optional.upgrade`；
- 默认候选数：5；
- `site_id` 和 `adzone_id` 从合法 PID 配置派生；
- 前端只接收规范化的商品字段，不解析淘联原始嵌套结构。

官方推广链接规则：

1. 优先 `item.publish_info.coupon_share_url`；
2. 缺失时使用 `item.publish_info.click_url`；
3. `//` 前缀规范化为 `https://`；
4. 缺少 `itemId`、标题、价格或有效官方链接的商品直接过滤；
5. 不拼接、改写或伪造链接；
6. 创建愿望时保存该链接快照；
7. 到期后直接使用快照，不二次转链。

价格展示规则：

- `final_promotion_price`：仅在语义成立时标为“预估到手价”；
- `zk_final_price`：标为“销售价”；
- `reserve_price`：可作为参考/标价，不是最终价；
- `predict_rounding_up_price`：只能标为有条件的“凑单参考价”，必须显示条件；
- 不得把任何预估值宣传为最终结算价；
- 一律补充“实际价格以淘宝结算页为准”。

禁止自动回退到已失败的旧接口 `16516/16518`。

### 8.2 知乎开放平台

- 正式产品每个愿望最多两次主搜索，专业向和体验向并行；
- 每次默认请求 5 条，最终每层展示 1～3 条；
- 摘要必须安全截断并按纯文本渲染；
- 保留标题、作者、认证、内容类型、原文链接等可追溯字段；
- 一层失败不阻塞另一层；
- 两层都没有结果时允许继续保管，但必须明确说明资料不足；
- 不允许用固定内容、淘宝文案或 AI 自造体验补位。

### 8.3 真实接口调用纪律

- UI 和状态机的重复测试优先使用明确标记的本地 fixture；
- 真实外部接口调用按测试用例计数，不在循环中无控制重试；
- 同一错误连续出现 2～3 次后停止，检查权限、配置、日志和供应商状态；
- 报告中只记录调用次数、阶段、HTTP/业务分类、数量和脱敏错误；
- 不记录完整推广链接、Access Secret、App Secret、PID、JWT、邮箱或供应商原始响应体。

---

## 9. 身份、数据和状态约束

### 9.1 身份模型

- 第一次访问无感创建 Supabase 匿名用户；
- 愿望归属于真实服务端 `auth.uid()`；
- 用户可自愿在当前匿名身份上绑定邮箱，绑定后用户 ID 不变；
- 已绑定用户通过当前页邮箱 OTP 在其他浏览器登录；
- 登录入口必须设置“不自动注册未知邮箱”；
- 新浏览器已有匿名愿望时，登录既有邮箱后通过受控函数迁移；
- 不做密码、手机号、愿望码、浏览器指纹、第三方 Cookie 或微信/知乎 OAuth。

### 9.2 浏览器存储边界

Supabase SDK 可以保存标准会话；`localStorage` 不得成为愿望正文、价格、统计或自制身份凭证的数据源。清除浏览器数据会退出登录，但不会删除服务端愿望；已绑定用户可以重新 OTP 登录，未绑定匿名用户可能无法证明原身份。

### 9.3 愿望状态

产品规格中的稳定业务状态包括：

```text
idle → product_searching → product_selecting
     → evidence_loading → evidence_ready/evidence_partial
     → custody_config → sealed → expired
     → purchase_ready 或 abandoned → archived
```

旧规格中的 `link_converting` 只具有历史语义。阶段 2 已冻结的状态机不使用该状态，不能让后续 UI 等待一个不会发生的服务调用。

### 9.4 服务端不可妥协项

- 前端不能指定或覆盖 `owner_id`；
- 前端提交的价格、到期时间、状态和累计金额不能直接信任；
- 最终决定必须使用条件更新和短事务；
- 同一愿望只能第一次有效决定；
- 放弃金额只能累计一次；
- 多浏览器重复提交返回同一个最终结果；
- 迁移和删除必须幂等；
- 外部接口调用不得放进数据库持锁事务。

---

## 10. 后续开发路线

每个阶段必须有独立的验收报告，例如 `PHASE2_ACCEPTANCE.md`。只有当前阶段退出条件全部满足，才能开始下一阶段。

### 10.1 阶段 2：项目骨架与静态流程（已完成记录）

目标：不依赖淘宝或知乎真实接口，使用明确标记的 fixture 走通全部产品页面和状态。

以下为已完成的工作顺序记录：

1. **先完成文档一致性修订**
   - 更新 `PROJECT_SPEC.md` 的阶段状态和旧转链措辞；
   - 确认状态机是否移除 `link_converting`；
   - 不改变冻结产品范围。
2. **保护阶段 1 诊断能力**
   - 不要直接把已验收诊断逻辑散落进正式 UI；
   - 可迁移为仅开发/受控环境可用的诊断入口；
   - 最终公网产品不能长期暴露会批量创建测试用户、测试愿望或消耗外部额度的诊断按钮。
3. **建立清晰模块边界**
   - 页面/视图；
   - 应用状态机；
   - API 适配器接口；
   - Auth/Wish 数据访问；
   - 金额与倒计时纯函数；
   - fixture。
4. **建立静态页面流程**
   - 首页/商品输入；
   - 搜索加载；
   - 商品候选；
   - 知乎双层卡；
   - 保管设置；
   - 封存/倒计时；
   - 到期决策；
   - 购买卡；
   - 放弃归档；
   - 我的愿望。
5. **实现静态状态机和错误状态**
   - 正常流程；
   - 无商品；
   - 知乎部分结果/无结果；
   - 保存失败；
   - 链接缺失；
   - 刷新恢复到最近稳定状态。
6. **实现响应式基础**
   - 单列手机优先；
   - 320、360、375、390、393、414、430 px；
   - 桌面 1280 px；
   - 安全区、动态视口、软键盘、系统字体放大；
   - `prefers-reduced-motion`。
7. **补最小自动测试**
   - 状态机合法/非法跳转；
   - 倒计时绝对时间计算；
   - 金额标签；
   - fixture 不会在生产环境冒充实时结果。

阶段 2 验收标准（已满足，证据见 `PHASE2_ACCEPTANCE.md`）：

- 代码完成：所有页面和状态都有可维护实现；
- 静态检查：`npm run check` 通过，新增测试通过；
- 接口验证：本阶段外部接口为“不适用”，不能用 fixture 宣称真实接口通过；
- 浏览器验证：桌面和重点手机宽度用 fixture 走通购买、放弃、刷新、错误流程；
- 退出条件：完全断开外部接口时仍可验证全流程，所有 fixture 明确标记为测试数据。

### 10.2 阶段 3：淘联真实商品搜索（已完成）

- 将现有 `products-search` 适配器接入正式候选页面；
- 完整规范化标题、图片、价格标签、官方推广链接和获取时间；
- 覆盖常见品类、型号、长尾、无结果、特殊字符、超时和权限错误；
- 候选只显示真实可推广商品，不显示佣金率；
- 外部图片失败不能阻塞流程。

退出条件：任意合法测试关键词能返回真实候选；无结果和错误状态诚实；密钥不出现在前端、日志和构建产物。

当前记录：本地实现、自动化、远程 `products-search`、真实淘宝接口和本地真实模式浏览器候选选择均已通过；冻结代码基线为 `8908331`，详见 `PHASE3_ACCEPTANCE.md`。

### 10.3 阶段 4：知乎双层内容

- 商品标题清洗；
- 专业/体验两路查询并行；
- 内容筛选、摘要纯文本化、安全截断；
- 双层卡和来源链接；
- 单层、双层无结果降级。

退出条件：至少三类不同商品完成实时双层验证，内容可追溯，部分失败不阻塞保管。

当前记录：远程 `zhihu-search`、三类商品真实专业/体验双层接口、本地异常情景和一条本地真实模式完整浏览器流程均已通过；冻结代码基线为 `07b6a4d`，详见 `PHASE4_ACCEPTANCE.md`。阶段 5 已进入本地实现，尚未做远程变更。

### 10.4 阶段 5：愿望保管与完整身份流程

- 24/48/72 秒；
- 按服务端绝对 `expiresAt` 恢复；
- 我的愿望时间线、多愿望并行、分页；
- 两个最终结局和幂等事务；
- 放弃金额防重复；
- 邮箱绑定、跨浏览器登录、匿名愿望迁移；
- 删除愿望、清空记录、退出设备、删除账户；
- 30 天匿名清理任务；
- 隐私模式和清除站点数据边界。

退出条件：刷新、返回、重复点击、并发浏览器、迁移、删除和金额边界全部通过。

当前记录：本地 Auth 服务、愿望 RPC 服务、开发测试情景、账户删除函数和 `PHASE5_SCHEMA_DRAFT.sql` 已提交；`PHASE5_ACCEPTANCE.md` 是最新本地验收依据。因 Supabase CLI 缺失，尚未生成或部署正式 migration；远程 RLS、SMTP、跨浏览器、Cron 和 WebView 均等待独立验收。阶段 6 未开始。

### 10.5 阶段 6：官方推广链接与购物卡

- 使用愿望创建时保存的官方 `promotionUrl`；
- 不二次转链；
- 购买卡不显示“购买成功”；
- 点击由用户明确触发；
- 链接缺失或失效时保留愿望并给出诚实提示；
- 商品一致、价格语义和结算页提示正确。

退出条件：链接有效、落地商品一致、无效链接不进入候选、失败不伪造成功。

### 10.6 阶段 7：视觉与浏览器矩阵

- 看山人格、动画和视觉完善；
- 重点手机尺寸、桌面、Safari、Chrome；
- 微信/知乎 iOS 与 Android 真实 WebView；
- 软键盘、切后台、返回手势、`pageshow`、`visibilitychange`；
- 淘宝和知乎外跳及平台拦截降级；
- 可访问性和控制台错误清零。

阶段 1 的 WebView 通过只证明诊断页和基础网络链路可用，不等于正式产品完整浏览器矩阵已通过。

### 10.7 阶段 8：最终部署和演示

- 环境配置复核；
- 公网真实接口回归；
- 安全和隐私检查；
- 额度、限速、缓存和熔断；
- 三次连续完整演示；
- 最终交付包和演示说明。

---

## 11. 统一验收标准

任何阶段都必须把四类状态分开写，禁止笼统使用“已完成”。

| 层级 | 可以证明什么 | 不能证明什么 |
|---|---|---|
| 代码完成 | 代码、迁移、配置文件已存在 | 不能证明可以构建或运行 |
| 静态检查 | 构建、语法、测试、扫描通过 | 不能证明真实接口或浏览器可用 |
| 接口验证 | 真实服务在授权环境返回预期结果 | 不能证明页面交互和 WebView 可用 |
| 浏览器验证 | 指定真实浏览器完成目标交互 | 不能自动代表其他浏览器、系统或接口异常分支 |

### 11.1 每阶段验收报告模板

```markdown
# 阶段 N 验收报告

## 阶段结论
- 通过 / 部分通过 / 阻塞
- 是否允许进入下一阶段

## 代码完成
- 文件与功能
- 未实现内容

## 静态检查
- 命令
- 结果

## 接口验证
- 环境、调用次数、用例
- 结果
- 未验证项

## 浏览器验证
- URL、设备、系统、浏览器/App 版本、视口
- 操作路径与观察结果
- 截图/日志证据位置

## 安全与数据
- 密钥扫描
- RLS/越权
- 日志脱敏
- 测试数据清理情况

## 已知差异和剩余风险

## 提交与部署
- commit
- workflow/run
- deployment URL
```

### 11.2 浏览器验证最低要求

- 写清入口 URL、设备、系统、浏览器/App 版本和视口；
- 验证页面身份、非白屏、无框架错误覆盖层、控制台错误；
- 每个主流程必须有真实交互后的状态证据；
- 桌面模拟不能替代微信/知乎真实 WebView；
- 受控浏览器失败而普通浏览器成功时分别记录；
- 截图不得包含邮箱、UID、JWT、密钥或完整推广链接；
- 外部链接必须核对落地商品，而不只核对 URL 发生变化。

### 11.3 安全验收最低要求

- `npm run check`；
- 检查 Git diff 和构建产物；
- 普通用户会话 RLS 越权验证；
- Edge Function JWT、CORS、输入长度、超时和脱敏错误；
- `.env` 忽略与本地凭据权限；
- 不记录完整邮箱、搜索隐私内容、供应商原始响应或推广跟踪参数；
- 删除、迁移、最终决定的幂等性；
- 外部 HTML 不直接插入 DOM。

---

## 12. 最终交付要求

只有 `PROJECT_SPEC.md` 的 Definition of Done 全部满足，才能称为最终 Demo 完成。最终至少交付：

### 12.1 源码和版本

- 可审查的 Git 历史；
- 明确的最终提交和发布标签；
- 无未解释的工作区修改；
- 锁文件完整；
- 不包含任何私密凭据；
- 历史诊断工具与正式产品入口隔离。

### 12.2 产品功能

- 任意合法关键词提交；
- 真实淘宝候选和用户明确选择；
- 可追溯知乎双层内容；
- 24/48/72 秒三档；
- 匿名使用、邮箱绑定和跨浏览器恢复；
- 我的愿望时间线和多愿望；
- 购买/放弃两个结局；
- 放弃金额不重复；
- 删除和账户控制；
- 有效官方推广链接与失败降级。

### 12.3 部署和运行

- 公网 HTTPS 地址；
- GitHub Actions 可重复构建；
- Supabase Auth/Postgres/Edge Functions 配置清单；
- 环境变量名称清单，无真实值；
- 本地启动和部署说明；
- 域名、SMTP、CORS、Redirect URLs 已核对；
- 若迁离 GitHub Pages，提供新托管地址和回滚方案。

### 12.4 测试证据

- 单元测试；
- 接口集成测试；
- RLS 和迁移安全测试；
- 桌面和重点手机尺寸；
- 微信/知乎 iOS 与 Android WebView；
- Safari/Chrome；
- 正常、部分结果、无结果、超时、刷新、重复提交、跨浏览器和删除；
- 三次连续完整演示；
- 各阶段验收报告。

### 12.5 安全、隐私和运营

- 密钥和构建产物扫描；
- 日志脱敏；
- 隐私说明、处理主体、联系方式、服务提供方、保存期限和删除方式；
- 匿名用户清理任务；
- 单用户限速、全站预算、缓存、并发上限和熔断；
- 监控、外部服务失败说明和应急操作；
- 不收集支付、姓名、手机号、地址，不将邮箱用于营销。

### 12.6 最终演示包

- 3～5 分钟演示脚本；
- 一个提前确认内容充足的排练商品，但不能把产品锁成固定商品；
- 现场网络和真实入口检查表；
- 接口失败时的诚实说明；
- 演示人员操作手册；
- 已知限制清单；
- 回滚到最后稳定版本的方法。

---

## 13. 当前技术债务和长期风险

接手团队必须把以下内容纳入路线图，不得误认为阶段 1 已解决：

1. **正式应用尚不存在**：当前是诊断页。
2. **测试不足**：目前以静态控制和人工验收为主，缺少系统化单元/E2E 套件。
3. **API 适配器仍较紧凑**：淘宝、知乎函数有重复 HTTP/CORS 逻辑，应在不改变行为的前提下逐步统一。
4. **链接校验需加固**：当前淘宝链接解析允许 `http` 或 `https`，而规格要求正式外部 URL 只允许 `https`；阶段 3/6 应补白名单/协议测试。
5. **价格映射不完整**：当前诊断只取最小价格字段，正式卡片必须实现价格语义和条件标签。
6. **知乎双层逻辑未实现**：当前函数只是单次基础搜索。
7. **迁移函数需生产级复核**：阶段 1 已验证当前流程，但正式上线前仍应检查目标账户必须为已验证邮箱身份、幂等审计和并发边界。
8. **远端配置未完全基础设施化**：Auth、SMTP、Secrets、Redirect URLs 和函数验证设置依赖控制台状态；必须维护不含密钥的配置清单。
9. **滥用保护未完成**：限速、缓存、全站预算和熔断尚未实现。
10. **数据生命周期未完成**：匿名用户 30 天清理、用户删除和隐私页面尚未实现。
11. **GitHub Pages 适用性风险**：如果平台规则、WebView 稳定性、子路径或商业外跳不合适，只迁移静态前端，不重写 Supabase 后端。
12. **浏览器矩阵未完成**：阶段 1 的真实 WebView 只验收诊断链路，不能替代最终 iOS/Android 全流程矩阵。
13. **外部平台会变化**：淘宝、知乎、Supabase、Resend 的权限、字段、额度和政策在每个正式发布前都要复核。

---

## 14. 新团队首周建议清单

### 第 1 天：只读接管

- [ ] 获得 GitHub、Supabase、Resend、DNS、淘联、知乎平台权限；
- [ ] 确认 `af3449e` 基线；
- [ ] 阅读本文件、阶段 1 报告和规格书；
- [ ] `npm ci && npm run check`；
- [ ] 不修改远端配置、不调用真实外部接口。

### 第 2 天：环境和运行核对

- [ ] 本地启动诊断页；
- [ ] 只核对公开配置加载；
- [ ] 查看 GitHub Pages、Actions 和 Supabase 项目状态；
- [ ] 建立不含值的远端配置清单；
- [ ] 确认真实密钥只在受控 Secrets 中。

### 第 3 天：阶段 2 已完成的规格一致性评审（历史记录）

- [x] 清理旧转链措辞和重复标题；
- [x] 冻结阶段 2 状态机；
- [x] 决定诊断入口的开发环境隔离方式；
- [x] 完成阶段 2 验收冻结，依据 `PHASE2_ACCEPTANCE.md`。

### 第 4～5 天：阶段 2 技术设计与实现（已完成记录）

- [x] 页面模块和状态机设计；
- [x] fixture 规范；
- [x] 单元测试策略；
- [x] 响应式和浏览器基础方案；
- [x] 阶段 2 验收矩阵与记录（`PHASE2_ACCEPTANCE.md`）；
- [x] 阶段 2 已完成并冻结在 `4c29383`。

---

## 15. 交接确认清单

移交方与接手方应共同确认：

- [ ] 接手方能访问仓库并看到提交 `af3449e`；
- [ ] 接手方能访问 GitHub Actions 和 Pages；
- [ ] 接手方能访问正确的 Supabase 项目，但没有把 secret/service-role 复制到本地前端；
- [ ] 接手方能访问 Resend 和 DNS 配置；
- [ ] 淘联和知乎平台的负责人、额度和权限边界已明确；
- [ ] 接手方理解转链 Skill 已停用；
- [ ] 接手方理解阶段 1 完成不等于最终 Demo 完成；
- [ ] 接手方理解每阶段必须分开报告四类证据；
- [ ] 接手方理解 `main` push 会自动部署公网；
- [ ] 接手方理解不能清理远端验收数据或重建项目；
- [x] 阶段 2 已完成并冻结在 `4c29383`；阶段 3 已完成并冻结在 `8908331`；阶段 4 已完成并冻结在 `07b6a4d`。
- [ ] 阶段 5 仅本地实现完成；正式 migration、远程 RLS/Auth/SMTP/Cron 与真实 WebView 尚未授权或验收；阶段 6 未开始。

完成以上确认后，新团队可以长期接管本项目。任何超出冻结范围的产品变化，应先形成书面变更说明、影响评估和新验收标准，再实施代码或基础设施变更。
