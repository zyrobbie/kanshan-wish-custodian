# 阶段 6 验收记录

> 状态：本地购物卡与提交版视觉已完成并冻结在本地提交中；本轮未调用外部接口、未部署、未推送。阶段 7/8 未开始。

> 视觉返工：当前产品名称统一为“看山·愿望寄存处”。首页直接以用户定稿的 `home-reference-original.png` 保持完整构图，仅叠加真实输入框、提交热区、愿望入口热区，以及明确要求的字符计数/系统手势条遮罩。此返工不改变购物卡、金额统计、愿望状态或远程边界。

## 代码完成

- 到期后“我还是想买”继续使用既有 `decide_custody_wish` 边界：正式模式由 RPC 写入 `purchased_intent`，随后只展示独立购物卡，不自动打开淘宝。
- 购物卡只从已保存愿望快照读取商品编号、标题、图片、销售价、预估到手价和 `promotionUrl`；不重新搜索、不二次转链、不拼接或替换链接。
- 外跳只在保存值本身为安全的 HTTPS 淘宝官方推广域名时创建真实 HTML `<a>`。链接缺失或无效时不生成按钮，并说明原因；本地验收只核对 `href`，未点击外部淘宝页面。
- 放弃结果页展示本次计划支出，以及服务端/本地存储返回的累计放下数量与金额；明确其不是实际财务收益。`purchased_intent` 不计入该累计，重复决定不会重复累计。
- 首页产品名称为“看山·愿望寄存处”；定稿图中的标题、商店、刘看山、对话框、表单外观和愿望入口外观保持原样，网页只覆盖必须真实交互的输入、提交和愿望入口。首页不展示动态愿望统计。其他流程中的守候、释然角色状态仍为展示层，不改变愿望生命周期。
- 动画为 CSS 的轻微呼吸/位移和结局过渡；`prefers-reduced-motion` 会关闭动画。
- 正式愿望列表隐藏删除账户入口；仅开发 `wishTest` 保留本地测试入口。
- 视觉返工后，首页使用整张定稿图作为视觉底图，不再重新拼装或重绘其画面；真实控件按定稿位置覆盖。字符计数与系统手势条由局部遮罩移除，辅助文案以不低于 14px 的 HTML 文本覆盖原文。其他页面仍使用透明刘看山 PNG。

## 静态检查

- `npm test`：56/56 通过。
- `npm run check`：通过；阶段 1–6 静态检查、测试、生产构建及构建产物扫描均通过。
- `npm run build`：通过。
- `git diff --check`：通过。
- 源码与构建产物密钥扫描：均通过；未发现凭据样式值、私钥占位或打包泄露。

## 外部接口验证

N/A。本轮淘宝、知乎、转链、Supabase、邮件和 Edge Function 调用均为 0 次。

## 浏览器验证

仅使用本地 Vite 明确标识的开发测试情景完成如下复验：

- 购买路径：到期后先写入 `purchased_intent`，购物卡创建的 `<a href>` 与保存快照完全一致；未点击外部淘宝链接。
- 放弃路径：展示本次金额、累计已放下数量和累计计划支出；`purchased_intent` 不计入，重复决定不重复累计。
- 刷新恢复：完整 reload 前后 `expiresAt` 完全相同，剩余时间继续减少。
- 无效保存链接：不生成淘宝 `<a>`，只显示诚实提示和返回入口。
- 图片失败：显示安全占位，不阻断商品、保管或结局操作。
- 视觉：已查看欢迎、守候、释然三种“看山”状态；展示状态不会改变愿望生命周期。
- 响应式：受控桌面 Chrome 本地视口 320、375、390、430px 均无横向溢出；这不是微信或知乎真实 WebView 验收。
- 控制台：购买、无效链接、刷新恢复与响应式页面的未处理 `error` 均为 0。
- 首页视觉稿返工复验：直接以 `home-reference-original.png` 保留定稿构图，仅检查真实控件覆盖、局部遮罩和交互；受控桌面 Chrome 的 320、375、390、430px 均无横向溢出，辅助文案计算字号不低于 14px，未发现字符计数、动态统计或黑色手势条。该受控桌面尺寸模拟不代表微信或知乎真实 WebView。

## 远程部署

未执行。不修改 Supabase、迁移、Edge Functions 或 Secrets。

## 延期项

- 阶段 5 的真实 OTP、正式 Pages 完整浏览器流程、跨浏览器身份、微信/知乎真实 WebView、30 天匿名清理和删除账户远程功能继续延期。
- 阶段 7/8、运营后台、年度账单、复杂动画与正式部署未开始。
