# 看山·愿望保管员 Demo

项目、功能与开发流程规格书

- 文档版本：`v1.3`
- 编写日期：`2026-08-15`
- 当前状态：阶段 3 实施完成，等待独立验收；阶段 4 未开始
- 适用范围：知乎 AI 创意黑客松可交互 Demo
- 产品主张：把冲动交给看山，把决定留给自己

---

## 1. 文档目的

本文档是后续开发、测试和验收的单一依据，用于在编码前明确：

1. Demo 要解决什么问题；
2. 测试者能完成哪些真实操作；
3. 淘宝联盟官方推广链接与知乎开放平台如何组成完整链路；
4. 哪些功能本轮必须实现，哪些明确不做；
5. 各页面、状态、数据字段和异常如何处理；
6. 开发按什么顺序推进，每一阶段如何验收；
7. 什么条件全部满足后，才能称为 Demo 完成。

本文档的产品范围、身份方案和部署架构已确认。阶段 1 已完成部署、身份持久化、RLS、邮件与目标 WebView 验收；阶段 2 已完成本地 fixture 静态流程并冻结，不接入新的真实业务接口或部署。若范围发生变化，应先更新本文档，再修改代码。

---

## 2. 项目概述

### 2.1 项目名称

**看山·愿望保管员**

### 2.2 一句话介绍

用户输入自己想买的商品，看山先从淘宝联盟找到真实商品，再从知乎提供“专业解读＋真实体验”两层证据；用户把愿望交给看山保管一段时间，冷静期结束后再决定购买或放弃。

### 2.3 Demo 的核心价值

Demo 不替用户判断“该不该买”，也不以高佣商品诱导购买。它做三件事：

1. 把模糊购买冲动落实为一个具体商品；
2. 在购买前补充可追溯的知乎内容证据；
3. 用一个有仪式感的等待过程，把即时下单变成二次确认。

### 2.4 Demo 形态

这是一个真实联网、可由任意测试者输入商品名称的 Web Demo，而不是固定商品分镜或离线静态页面。

用户看到的是 H5/网页界面；为保护平台密钥并调用外部接口，系统必须包含轻量后端或 Serverless Function，不能只由一个静态 HTML 文件组成。

### 2.5 官方活动前提

- 本项目用于知乎官方活动；
- 已获得平台授权；
- 本文档不再重复讨论参赛授权与知乎内容使用授权；
- 开发仍应保留内容来源、作者和原文链接，保证 Demo 可追溯。

---

## 3. 已确认事实与当前状态

### 3.1 已确认可用

| 能力 | 当前状态 | 开发结论 |
|---|---|---|
| 知乎官方 Skill | 已安装，版本 `0.2.1` | 可作为调试与能力验证工具 |
| 知乎 CLI | 已安装，版本 `0.2.0` | 开发应优先接官方 HTTP API，CLI 可用于独立核验 |
| 知乎搜索额度 | 当前邀测额度为每天 5000 次 | Demo 规模充足，仍需处理限额错误 |
| 淘宝联盟账号与推广位 | 已确认可用 | 固定使用“知乎／罗比”推广位 |
| 淘联升级版搜索 | `27939` 已审批并真实调用成功 | 作为唯一商品搜索主接口 |
| 淘联升级版接口 | `taobao.tbk.dg.material.optional.upgrade` | 正式开发依赖此接口 |
| 淘联返回字段 | 已实测取得营销 ID、标题、主图、价格、推广链接 | 足以形成真实商品候选卡 |
| 淘宝联盟官方推广链接 | 已由升级版物料搜索返回 | `coupon_share_url` 优先，`click_url` 后备；仅保留有有效官方链接的候选 |
| 转链 Skill | 已安装但本阶段停用 | 源码保留；不作为 Demo 主路径、不开启调用或验收重试 |
| 转链真实业务调用 | 不作为当前验收项 | 历史诊断未形成有效落地，不得表述为已通过 |
| 凭证文件安全 | 已确认 | `.env.taobao.local` 与转链凭证权限均为 `600`，且被 Git 忽略 |
| 本地验证脚本 | 已存在并通过语法检查 | 用于淘联接口独立冒烟测试 |

### 3.2 明确不再依赖

| 权限/接口 | 状态 | 处理方式 |
|---|---|---|
| `16516` | 旧版搜索仍返回权限不足 | 不进入业务代码 |
| `16518` | 旧版精选仍返回权限不足 | 不进入业务代码 |
| KA 媒体选品 Skill | 申请失败 | 不再等待、不再作为依赖 |

### 3.3 外部接口开工闸门

外部接口开工闸门：淘联升级版搜索和本地凭证权限已通过；正式 Demo 直接使用升级版物料搜索返回的官方推广链接。转链 Skill 已移出主路径与验收门槛；官方链接的浏览器落地仍须按阶段 1 限额单独核对。

### 3.4 阶段状态

| 阶段 | 状态 | 当前边界 |
|---|---|---|
| 阶段 1：部署与身份持久化 | 已完成 | 证据冻结在 `PHASE1_ACCEPTANCE.md`，诊断能力保留在 `diagnostic.html` |
| 阶段 2：静态产品流程 | 已完成 | 验收证据见 `PHASE2_ACCEPTANCE.md`；冻结代码基线为 `4c29383`；仅本地 fixture、状态机与浏览器结构验收，不调用外部业务接口、不部署 |
| 阶段 3：淘联真实商品搜索 | 实施完成，等待独立验收 | 本地实现与测试证据见 `PHASE3_ACCEPTANCE.md`；真实函数部署与真实接口验收因本机 Supabase CLI 缺失而阻塞 |
| 阶段 4 及后续 | 未开始 | 不得在阶段 3 接入知乎、转链或正式愿望业务流程 |

---

## 4. 产品目标与非目标

### 4.1 本轮必须达到的目标

1. 任意测试者可以输入一个合法的商品名称、品牌或型号；
2. 系统实时查询淘宝联盟并返回真实商品候选；
   - 用户可以搜索任意商品名称，但候选仅来自淘宝联盟当次返回的可推广商品池；系统不承诺任意指定淘宝商品、分享链接或商品 ID 都能生成推广链接；
3. 用户必须选择一个具体商品，系统不得静默替用户决定；
4. 系统实时搜索知乎，形成可追溯的双层解读卡；
5. 用户可以选择 24、48 或 72 小时保管时长；
6. Demo 中分别压缩为 24、48 或 72 秒，并明确标识为演示时间；
7. 倒计时结束后，用户可以选择“还想买”或“不要了”；
8. “还想买”时生成或使用有效淘宝联盟推广链接；
9. “不要了”时记录该商品创建愿望时的价格快照；
10. 愿望和用户归属由服务端持久化；首次使用无需注册，同一浏览器自动回到愿望保管箱，用户可自愿绑定邮箱后跨浏览器查看同一批愿望；
11. 全流程在手机和桌面浏览器均可完成，其中微信和知乎 App 内置浏览器为首要移动运行环境；
12. 页面必须适配常见手机宽度、刘海/灵动岛安全区、软键盘和内置浏览器可视区域变化；
13. 淘宝、知乎、转链及 Supabase 私密密钥不得出现在前端、浏览器网络面板和公开仓库；仅允许公开设计的 Supabase URL 与 publishable key 出现在前端，并必须配合 RLS。

### 4.2 本轮明确不做

1. 图片识别或拍照搜商品；
2. 扫商品条码；
3. 京东联盟、拼多多或多平台比价；
4. 真实 24 小时系统推送；
5. 密码注册、手机号登录、第三方社交登录、昵称头像和复杂账户中心；
6. 订单状态、实际成交、佣金或退款追踪；
7. 真实年度账单生成与分享海报；
8. 后台运营系统；
9. 对知乎作者身份或“真实购买者”进行平台外认证；
10. 自动替用户下单、支付或提交订单；
11. 使用高佣金作为商品排序目标；
12. 爬取淘宝、知乎或其他网站页面。

### 4.3 可展示但不承诺完整实现

- 年度愿望账单可作为片尾概念预告；
- 首页可以保留图片搜索入口的视觉占位，但必须标注“暂未开放”且不可伪装可用；
- 可以展示“看山保管过多少愿望”的保管箱统计；未绑定邮箱时仅代表当前匿名用户，绑定后代表该邮箱身份下的愿望数据。

---

## 5. 目标用户与使用场景

### 5.1 目标用户

- 正在犹豫是否购买某件商品的人；
- 希望先查资料、过一段时间再决定的人；
- 在黑客松现场体验完整产品流程的测试者和评委。

### 5.2 典型场景

用户夜间突然想买一台便携咖啡机。他输入“便携咖啡机”，从真实淘宝商品中选中一个具体型号。看山展示知乎上与该产品或品类相关的专业说明和长期使用体验。用户把愿望保管 24 小时；Demo 用 24 秒模拟。时间结束后，用户选择购买或放弃。

### 5.3 用户成功标准

一次成功体验应满足：

1. 用户能搜到与输入明显相关的商品；
2. 用户知道自己选择的是哪件具体商品、价格是多少；
3. 双层卡里的内容可以点击回到知乎原文；
4. 用户清楚当前等待是 Demo 模拟；
5. 用户的最终选择被正确记录；
6. 购买链接能打开正确商品，放弃金额不会重复累计。

---

## 6. 产品原则

### 6.1 用户决定

系统不自动选择商品、不替用户决定购买，也不自动打开购物页面。

### 6.2 证据优先

知乎内容必须保留作者、标题、摘要和原文链接。系统可以组织内容，但不能把来源隐藏成一个不可追溯的 AI 结论。

### 6.3 中立优先

淘宝商品候选默认按接口相关性展示，不按佣金率排序；前端不展示佣金率，不采用淘联自动生成的强营销文案。

### 6.4 简单优先

本轮只做文字商品搜索。任何会增加账号体系、图像识别、多平台适配或真实推送复杂度的功能均延后。

### 6.5 可靠优先

任何关键接口失败时，页面必须给出明确状态和重试入口，不能用假数据冒充实时结果。

### 6.6 演示诚实

24 小时压缩为 24 秒时必须明确写出“演示模式”；页面不把模拟倒计时说成真实推送。

---

## 7. 完整用户流程

### 7.1 主流程

```text
进入首页
  ↓
输入商品名称、品牌或型号
  ↓
淘联升级版接口返回真实商品候选
  ↓
用户选择一件具体商品
  ↓
系统并行发起两次知乎搜索
  ↓
展示“专业解读＋真实体验”双层卡
  ↓
用户选择 24 / 48 / 72 小时
  ↓
愿望封存，Demo 按秒倒计时
  ↓
倒计时结束，用户选择结局
  ├─ 还想买 → 使用已保存的官方推广链接 → 淘宝购物卡 → 用户主动跳转
  └─ 不要了 → 归档愿望 → 累计放弃购买的商品金额
```

### 7.2 商品搜索规则

1. 输入框接受中文、英文、数字及常见型号符号；
2. 去除首尾空格，连续空格折叠为一个；
3. 空输入不能提交；
4. 建议限制为 2～80 个字符；
5. 每次只展示前 5 个有效候选；
6. 无营销 ID、无标题、无价格的结果不进入候选列表；
   - 只有淘宝联盟本次搜索返回、且含可用营销 `item_id` 的商品才是有效候选；用户自行粘贴的淘宝链接不是候选来源，也不应作为“任意商品可推广”的承诺；
7. 无图片时允许使用统一占位图，但仍可选择；
8. 搜索结果必须显示“数据来自淘宝联盟”；
9. 每件候选至少显示标题、图片和当前价格；
10. 用户选定后保存价格快照，后续统计不受页面刷新影响。

### 7.3 淘宝商品选择规则

- 系统不默认选中第一件商品；
- 用户必须明确点击某件商品；
- 选中后展示确认状态；
- 用户可以返回重选；
- 创建愿望后不允许无提示地更换商品；
- 如需更换，必须退出当前愿望并重新创建。

### 7.4 知乎双层搜索规则

选择具体商品后，系统从商品标题中提取适合搜索的核心名称，去除常见促销词、数量词和无意义修饰词。优先保留品牌、系列、型号和品类。

建议查询模板：

```text
专业向：{核心商品名} 值得买吗 测评 适合谁
体验向：{核心商品名} 后悔 积灰 长期使用体验
```

每个查询最多请求 5 条，最终每层展示 1～3 条最相关内容。

### 7.5 双层卡展示规则

#### 专业解读

优先考虑：

- 与具体品牌、型号或品类直接相关；
- `AuthorityLevel` 较高；
- 作者认证、标题和正文能够支持功能或适用人群说明；
- 内容不是只有促销链接的空泛种草。

#### 真实体验

优先考虑：

- 正文出现使用时长、具体场景、清洗、维护、闲置、故障等体验信息；
- 标题或正文与“长期使用、后悔、积灰、优缺点”等意图相关；
- 内容包含明确个人经验，而不是纯参数罗列。

#### 共同展示字段

- 内容标题；
- 作者名称；
- 作者认证文案（如果存在）；
- 内容类型；
- 经过安全截断的原始摘要；
- 赞同数或权威等级（如果适合展示）；
- 原文链接；
- “来源：知乎”标识。

#### 内容不足时

- 其中一层有结果：展示该层，并明确另一层“暂未找到足够相关内容”；
- 两层都没有结果：允许用户继续保管商品，但页面明确说明“本次未找到足够知乎资料”；
- 不得用淘宝商品文案冒充知乎真实体验；
- 不得临时插入固定样例冒充实时结果。

### 7.6 保管时长

| 用户选择 | Demo 倒计时 | 页面文案 |
|---|---:|---|
| 24 小时 | 24 秒 | 演示模式：24 小时压缩为 24 秒 |
| 48 小时 | 48 秒 | 演示模式：48 小时压缩为 48 秒 |
| 72 小时 | 72 秒 | 演示模式：72 小时压缩为 72 秒 |

默认选择 24 小时。倒计时基于绝对时间戳计算，不依赖 `setInterval` 累加，从而避免页面切换或浏览器降频导致明显漂移。

### 7.7 到期后的两个结局

#### 结局 A：还想买

1. 用户点击“还想买”；
2. 系统直接使用愿望记录中保存的淘宝联盟官方 `promotionUrl`，不在到期时二次转链；
3. `promotionUrl` 仅可由升级版物料接口的 `item.publish_info.coupon_share_url` 取得，`item.publish_info.click_url` 为后备；以 `//` 开头时规范化为 `https://`；
4. 不拼接、改写或伪造推广链接；无有效官方链接的商品不得进入候选；
5. 展示购物卡，用户再次点击后才打开淘宝；
6. 记录愿望状态为 `purchased_intent`，仅表示用户选择前往购买，不代表真实成交；
7. 不增加“放弃购买金额”。

#### 结局 B：不要了

1. 用户点击“不要了”；
2. 愿望状态变为 `abandoned`；
3. 将创建愿望时的有效价格快照计入当前用户愿望记录的累计；
4. 同一愿望只能累计一次；
5. 文案使用“这次放下的商品金额”，不声称为真实财务节省；
6. 用户可以返回首页创建新愿望。

---

## 8. 页面与功能规格

### 8.1 首页／商品输入页

必须包含：

- 产品名称与一句话主张；
- 看山角色视觉；
- 商品名称输入框；
- 明确的“交给看山看看”按钮；
- 2～3 个可点击搜索示例，只用于填充输入框，不作为固定数据；
- 当前愿望保管箱的愿望数量和累计放弃商品金额轻量入口；
- 图片搜索入口如保留，必须禁用并标注“暂未开放”。

### 8.2 搜索加载页

加载阶段分步显示：

1. 正在寻找真实商品；
2. 正在整理候选；
3. 搜索完成。

超过合理等待时间后显示取消和重试，不无限转圈。

### 8.3 商品候选页

每张商品卡至少包含：

- 商品图片；
- 标题；
- 当前价格；
- 券后价或优惠信息（存在时）；
- 来源标识；
- 选择按钮。

候选页不展示佣金率，不使用“爆买”“必入”等强营销词。

### 8.4 知乎双层解读页

页面顺序：

1. 已选商品摘要；
2. 专业解读；
3. 真实体验；
4. 原文来源入口；
5. 保管提议。

读取外部内容时必须使用纯文本渲染，不能直接把接口返回的 HTML 插入 DOM。

### 8.5 保管设置页

- 24、48、72 三个时间选项；
- 默认 24；
- 明确写出演示压缩规则；
- 用户确认后才创建愿望记录；
- 创建成功后进入封存动画。

### 8.6 封存与倒计时页

- 商品卡进入保管箱；
- 封条、时间戳或盖章动画；
- 展示剩余秒数和原始选择时长；
- 展示“演示模式”标签；
- 页面刷新后恢复同一愿望；
- 倒计时结束自动进入到期状态，但不自动选择结局。

### 8.7 到期决策页

- 商品摘要；
- “还想买”和“不要了”两个同等清晰的按钮；
- 不通过颜色、尺寸或位置明显诱导其中一个选择；
- 选择后防止重复提交。

### 8.8 购买卡页

- 商品图片、标题、价格快照；
- 当前可用推广链接；
- “去淘宝看看”按钮；
- “最终价格以淘宝页面为准”提示；
- 返回首页入口；
- 不显示“购买成功”。

### 8.9 放弃归档页

- 柔和的完成反馈；
- 本次放下的商品金额；
- 当前愿望保管箱的累计金额；
- 已放下愿望数量；
- 返回首页入口；
- 年度账单概念预告可以作为非交互彩蛋。

### 8.10 愿望保管箱、匿名使用与跨浏览器同步

最低要求：

- 首页提供“我的愿望”入口，进入后以时间线或卡片列表展示当前保管、待决定、已购买意向和已放弃的历史记录；
- 首次访问时由 Supabase Auth 无感创建匿名用户，愿望直接归属于该服务端用户 ID；用户无须先注册、登录、复制凭证或输入愿望码；
- 创建愿望、刷新页面、关闭后重新打开，都自动回到当前用户的保管箱；`localStorage` 不保存愿望正文、价格统计或自制身份凭证；
- 用户完成第一个愿望后，只展示一次非阻断的轻提示：“绑定邮箱，换浏览器也能找到愿望”；不绑定也可以继续完整使用当前浏览器；
- “我的愿望”页提供“同步到其他浏览器”入口。用户输入邮箱并完成一次验证码验证后，将当前匿名身份升级为永久邮箱身份，原有愿望留在同一账户中，不另建空账户；
- 已绑定用户在新浏览器、微信或知乎内置浏览器中输入同一邮箱和一次性验证码，即可进入同一愿望时间线；不设置密码，不提供愿望码；
- 邮箱仅用于身份验证和找回愿望，不用于营销订阅；页面必须明确说明用途；
- 隐私模式允许匿名完成流程，但关闭窗口后可能失去匿名会话；若已绑定邮箱，可再次用邮箱验证码登录；未绑定时无法可靠找回；
- 微信和知乎内置浏览器可能保存不同的本地会话，但同一已验证邮箱登录后必须看到相同服务端愿望；
- 若新浏览器在登录前已经创建匿名愿望，登录已有邮箱账户时不得静默覆盖：应在验证完成后把当前匿名愿望安全迁入该邮箱账户，并显示迁入结果；重复数据按愿望 ID 去重；
- 允许用户删除单条愿望、清空愿望记录或删除账户，均须二次确认；删除账户同时清理关联愿望和邮箱身份；
- 不使用浏览器指纹、IP 地址、设备特征或第三方 Cookie 识别用户；
- 本轮不做密码、手机号、微信/知乎 OAuth 和复杂个人资料。

### 8.11 多愿望并行规则

- 每个用户最多同时保管 5 个未完成愿望；已完成历史不占用名额；
- 每个愿望使用独立的 `expiresAt`，创建新愿望不暂停或覆盖旧愿望；
- “我的愿望”排序固定为：待决定、保管中、已完成；同组内按最近到期或最近操作排序；
- 首页轻量显示“保管中数量”和“待决定数量”，不强制用户先处理旧愿望；
- 同一愿望在多个浏览器同时操作时，服务端第一次有效最终决定为准，后续请求返回既有结果；
- 已放弃金额只能在状态首次从 `expired` 变为 `abandoned` 的同一数据库事务中计入；
- 迁移匿名愿望时按愿望 ID 去重，已完成状态不得回退，金额不得再次累计；
- 历史记录采用分页或游标读取，首版不一次加载全部记录。

---

## 9. 应用状态机

| 状态 | 含义 | 允许的下一状态 |
|---|---|---|
| `idle` | 等待输入商品 | `product_searching` |
| `product_searching` | 淘联搜索中 | `product_selecting`、`product_error` |
| `product_selecting` | 等待用户选择具体商品 | `evidence_loading`、`idle` |
| `evidence_loading` | 两路知乎搜索中 | `evidence_ready`、`evidence_partial` |
| `evidence_ready` | 双层内容完整 | `custody_config` |
| `evidence_partial` | 内容不足但可以继续 | `custody_config`、`product_selecting` |
| `custody_config` | 选择保管时长 | `sealed` |
| `sealed` | 倒计时进行中 | `expired` |
| `expired` | 等待用户做最终决定 | `purchase_ready`、`abandoned` |
| `purchase_ready` | 购物卡已就绪 | `archived` |
| `abandoned` | 已放弃并完成金额累计 | `archived` |
| `archived` | 历史记录 | `idle` |

错误状态必须可返回上一个可操作状态，不能让用户卡死。

---

## 10. 数据模型

### 10.1 ProductCard

```json
{
  "provider": "taobao",
  "itemId": "营销ID",
  "title": "商品标题",
  "imageUrl": "https://...",
  "price": 124.87,
  "finalPrice": 39.89,
  "priceLabel": "预估到手价",
  "bundlePrice": 34.91,
  "bundlePriceCondition": "需凑单",
  "couponAmount": null,
  "shopName": null,
  "promotionUrl": "https://...",
  "query": "露营灯",
  "fetchedAt": "2026-08-11T12:00:00.000Z"
}
```

规则：

- 金额统一转为数字；
- `promotionUrl` 只来自淘宝联盟升级版物料响应的 `item.publish_info.coupon_share_url`，缺失时使用 `item.publish_info.click_url`；`//` 开头的值规范化为 `https://`；
- 无 `itemId`、标题、价格或有效官方 `promotionUrl` 的记录不进入候选；不得拼接、改写或伪造推广链接；
- `price` 对应销售价；`finalPrice` 优先取接口的 `final_promotion_price`；
- `finalPrice` 不存在时使用 `price`，此时标签改为“销售价”，不得称为“到手价”；
- `predict_rounding_up_price` 只能作为附属的“凑单参考价”展示，必须同时显示条件，不得作为购物卡主价格；
- 所有预估价格旁固定显示“实际价格以淘宝结算页为准”；
- 淘宝页面价格可能因所选 SKU、账号权益、地区、活动时点和优惠叠加条件而变化；本地快照只记录创建愿望时的接口结果；
- 前端不依赖淘联原始嵌套字段；
- `itemId` 使用升级版返回的营销 ID；
- 服务端只返回页面需要的字段。

### 10.2 EvidenceItem

```json
{
  "layer": "expert",
  "title": "内容标题",
  "authorName": "作者",
  "authorBadgeText": "认证信息",
  "contentType": "Answer",
  "excerpt": "安全截断后的摘要",
  "url": "https://www.zhihu.com/...",
  "voteUpCount": 0,
  "authorityLevel": "2",
  "editTime": 0
}
```

### 10.3 WishRecord

```json
{
  "id": "服务端愿望ID",
  "ownerId": "Supabase Auth 用户ID",
  "product": {},
  "evidence": {
    "expert": [],
    "experience": []
  },
  "custodyHours": 24,
  "demoDurationSeconds": 24,
  "createdAt": 0,
  "expiresAt": 0,
  "status": "sealed",
  "decisionAt": null,
  "promotionUrl": null,
  "countedAmount": 0
}
```

愿望必须归属于 Supabase Auth 用户。浏览器提交的金额、状态、到期时间和 `ownerId` 均不得直接信任；数据库通过当前登录用户的 `auth.uid()` 和 Row Level Security（RLS）确定归属。

创建愿望时必须将所选候选的 `promotionUrl` 原样保存为愿望快照；24 秒演示到期后“还想买”只使用该快照值，不再调用转链服务。

### 10.4 UserIdentity

```json
{
  "id": "Supabase Auth 用户ID",
  "isAnonymous": true,
  "email": null,
  "createdAt": 0,
  "lastSeenAt": 0,
  "emailLinkedAt": null
}
```

规则：

- 第一次使用调用 Supabase 匿名登录，产生真实服务端用户 ID，但不要求邮箱、密码或个人资料；
- 匿名用户和邮箱用户都属于 `authenticated` 角色，数据库 RLS 必须额外按 `auth.uid() = owner_id` 隔离数据；
- 用户自愿绑定邮箱后，在原匿名用户上完成身份升级，`ownerId` 不变，既有愿望无需搬迁；
- 新浏览器使用邮箱一次性验证码登录；验证码仅用于身份验证，不是愿望码，也不长期保存；
- 登录入口必须区分“已有保管箱登录”和“首次绑定邮箱”，避免误创建重复账户；
- 已有本地匿名愿望再登录邮箱账户时，通过服务端一次性迁移流程把愿望转移到已验证账户；迁移必须原子化、可重试、可审计且不复制金额；
- Supabase 标准会话可以保存在浏览器本地，但它只用于保持登录，不是愿望数据源；清除浏览器数据会退出登录，服务端愿望不会被删除；
- 邮箱只由 Auth 服务保存，业务表不重复存储；页面和日志不得展示或记录完整邮箱；
- 匿名用户未绑定邮箱且清除全部浏览器数据后，无法证明原身份，因而无法找回原愿望；
- 匿名用户每次成功创建、读取或决定愿望时，服务端更新可供清理任务使用的 `lastSeenAt`；仅加载静态首页不刷新保留期；
- 页面不提供愿望码、永久恢复链接或自行设计的密码体系。

### 10.5 WishSummary

```json
{
  "wishCount": 0,
  "abandonedCount": 0,
  "purchaseIntentCount": 0,
  "abandonedListedAmount": 0
}
```

统计应由同一 `ownerId` 下的愿望记录重新计算，不单独维护容易重复累加的计数器。

### 10.6 数据库约束与索引

最低要求：

- `wishes.id` 使用 UUID 主键；
- `wishes.owner_id` 外键指向 Supabase Auth 用户，并为用户删除设置明确级联策略；
- `status`、`custody_hours`、金额和时间字段设置数据库约束，不只依赖前端校验；
- 为“当前用户＋状态＋到期时间”和“当前用户＋创建时间”建立复合索引；
- 所有愿望表、统计查询和迁移函数都必须受 RLS 或受控的安全函数保护；
- 最终决定使用条件更新，例如仅允许 `expired` 转为终态，并在短事务中返回最终结果；
- 数据迁移、删除和最终决定均使用幂等键或唯一约束防止重复执行；
- 不在数据库事务中等待淘宝、知乎、邮件或转链接口。

### 10.7 数据保留规则

- 未绑定邮箱的匿名用户：自最后一次成功愿望操作起保留 30 天；
- 匿名用户超过 30 天未活动后，由每日清理任务删除 Auth 用户；相关愿望通过外键 `on delete cascade` 一并删除；
- Supabase 不提供匿名用户自动清理，本项目必须使用受控的 Supabase Cron/数据库函数实现，不能只写在文案里；
- 已绑定邮箱用户：默认保留至用户主动删除账户；若活动结束后决定整体下线，至少提前 7 天在页面公示最终删除日期；
- 用户删除单条愿望或清空愿望记录后立即从业务表删除；删除账户时删除 Auth 用户并级联清理业务数据；
- 删除后的记录不进入产品可恢复区；平台备份的实际保留遵循届时采用的 Supabase 服务条款，并在隐私说明中如实披露；
- 本 Demo 不以搜索词建立长期画像；接口安全日志最长保留 30 天，且邮箱脱敏、IP 仅保留不可逆限速哈希。

---

## 11. 技术架构

### 11.1 已确认技术基线

为兼顾可靠性、跨浏览器体验和开发成本，本轮固定采用：

- 前端：原生 HTML、CSS、JavaScript；
- 开发构建：Vite 的 Vanilla 模板或等价轻量开发服务器；
- 前端部署：GitHub 仓库管理源码，GitHub Actions 构建并发布到 GitHub Pages；
- 身份服务：Supabase Auth，首次匿名登录，可自愿升级为邮箱验证码身份；
- 数据持久化：Supabase Postgres，愿望以 `owner_id` 关联用户，业务数据不写入 GitHub 或浏览器本地存储；
- 服务端：Supabase Edge Functions，负责淘宝、知乎、账户迁移和其他需要密钥或可信计算的请求；转链适配器仅保留源码，不在 Demo 路径调用；
- 外部数据：知乎开放平台、淘宝联盟升级版接口；商品外跳使用后者返回的官方推广链接；
- 测试：Node 内置测试或轻量单元测试工具，加浏览器端端到端测试。

不使用 React、密码体系、复杂状态管理库或自建数据库服务器。GitHub Pages 只承载静态前端；用户数据、服务端函数和密钥全部位于 Supabase。生产环境需为 Supabase Auth 配置可向真实测试者送达验证码的邮件服务，不能依赖仅供开发测试的受限邮件通道。

### 11.2 架构图

```text
GitHub Pages 前端
  ├─ 页面与动画
  ├─ “我的愿望”时间线
  ├─ 匿名会话
  ├─ 可选邮箱绑定/验证码登录
  └─ 倒计时与非敏感界面缓存
          ↓ HTTPS
Supabase Auth
  └─ 匿名用户、邮箱身份与多浏览器会话
          ↓ 用户 JWT
Supabase Edge Functions
  ├─ 输入校验与超时控制
  ├─ TaobaoSearchAdapter
  ├─ ZhihuSearchAdapter
  ├─ WishService
  ├─ 匿名愿望迁移
  ├─ 限速与幂等控制
  └─ 统一错误与日志脱敏
          ↓
Supabase Postgres + RLS
  └─ 用户隔离的愿望、状态、价格快照和证据摘要
          ↓
外部服务
  ├─ 淘联 27939 升级版商品搜索
  ├─ 知乎开放平台搜索 × 2
```

### 11.3 适配器边界

业务代码只依赖以下内部能力：

```text
searchProducts(query) → 返回已规范化的官方 promotionUrl
searchEvidence(product)
createWish(wish)
listMyWishes()
decideWish(wishId, decision)
linkEmail(email, otp)
signInWithEmailOtp(email, otp)
migrateAnonymousWishes(targetUser)
```

外部平台参数、签名、字段转换和错误码全部封装在适配器内部。前端不得直接理解淘宝或知乎原始响应结构。

### 11.4 后端接口基线

#### POST `/api/products/search`

请求：

```json
{ "query": "便携咖啡机" }
```

响应：

```json
{
  "ok": true,
  "products": []
}
```

#### POST `/api/evidence/search`

请求：

```json
{
  "itemId": "营销ID",
  "title": "商品标题",
  "originalQuery": "便携咖啡机"
}
```

响应：

```json
{
  "ok": true,
  "normalizedQuery": "品牌 型号 便携咖啡机",
  "expert": [],
  "experience": []
}
```

> 已停用的历史路径：`/api/links/convert` 与淘客转链 Skill 源码仅为既有历史诊断保留，不属于 Demo 正式业务路径、降级策略或阶段验收门槛。创建愿望时保存升级版物料搜索返回的官方 `promotionUrl`，到期后直接使用该快照，不二次转链。

#### POST `/api/wishes`

由当前 Supabase Auth 用户创建服务端愿望记录。服务端从已验证 JWT 取得 `auth.uid()`，不得接受前端指定 `ownerId`。响应只返回愿望展示数据，不返回服务端密钥。服务端必须重新校验价格快照和状态字段。

#### GET `/api/wishes`

根据当前 Auth 用户返回“我的愿望”时间线和统计。没有会话时前端先无感完成匿名登录，再发起请求。

#### GET `/api/wishes/:id`

读取当前用户的指定愿望；数据库 RLS 必须阻止仅凭愿望 ID 越权读取其他用户的数据。

#### POST `/api/wishes/:id/decision`

提交当前用户愿望的最终决定。服务端执行 RLS 归属校验、条件更新和幂等保护，确保“不要了”的金额只计算一次，重复请求返回同一最终状态。

#### Supabase Auth：匿名登录与邮箱绑定

- 首次访问：`signInAnonymously()`；
- 绑定邮箱：在当前匿名用户上添加并验证邮箱身份，不创建新的空账户；
- 跨浏览器登录：发送邮箱 OTP，用户在当前页面输入一次性验证码；
- 验证码请求按邮箱哈希、IP 和设备会话组合限速；错误信息不透露邮箱是否已注册。

#### POST `/api/account/migrate-anonymous-wishes`

当新浏览器已经产生匿名愿望、随后登录既有邮箱账户时调用。服务端同时验证匿名会话和目标邮箱会话，在短事务中原子迁移愿望并按愿望 ID 去重；外部 API 调用不得放在迁移事务内。

### 11.5 密钥存放

服务端环境变量至少包括：

- `TAOBAO_APP_KEY`
- `TAOBAO_APP_SECRET`
- `TAOBAO_PID`
- `TAOBAO_ADZONE_ID`
- `ZHIHU_ACCESS_SECRET`
- 转链 Skill Access Key（仅保留历史适配器时使用；不属于当前 Demo 路径）

禁止：

- 写入前端代码；
- 写入 HTML；
- 提交 Git；
- 在错误响应中返回；
- 在日志中打印完整值。

### 11.6 部署方式

默认部署形态为：

1. 源码托管在 GitHub；
2. GitHub Actions 构建 Vite 前端并发布到 GitHub Pages；
3. GitHub Pages 只托管 HTML、CSS、JavaScript 和公开静态资源，不保存用户数据、不执行服务端代码、不持有私密密钥；
4. Supabase 托管 Auth、Postgres 和 Edge Functions；
5. 前端只包含 Supabase URL 和 publishable key；所有淘宝、知乎、转链及 Supabase secret key 只放 Supabase 项目 Secrets；
6. Postgres 的愿望表启用并强制执行 RLS，所有读写策略均使用 `auth.uid() = owner_id`；
7. GitHub Pages 与 Supabase 之间只走 HTTPS，并只允许正式 Pages 域名、必要的本地域名访问函数；
8. Supabase Auth 的 Site URL、Redirect URLs 和邮件模板固定指向正式 GitHub Pages 地址；
9. 若使用 GitHub Pages 项目子路径，Vite `base`、登录回跳和前端路由必须包含仓库路径；优先使用单页内状态或 Hash 路由，避免 Pages 刷新 404；
10. 正式演示前完成 GitHub Pages 公网地址、邮件送达、微信 WebView、知乎 WebView和外部浏览器验证；
11. 为避免仓库或域名变化导致登录回跳失效，演示地址确定后冻结；如有条件，使用并验证自有域名且强制 HTTPS。

GitHub Pages 适配性风险：GitHub Pages 是静态站点托管，不承担本项目后端；其官方使用限制还提示不适合以促成商业交易为主要目的的网站。本项目虽是官方活动 Demo、不会在站内支付，但包含淘客购买跳转。上线前必须核对该用途是否符合规则；源码和构建流程保持平台无关，若 Pages 不适合，则只替换前端托管位置，不改 Supabase 后端和业务架构。

### 11.7 公开 Demo 的额度与滥用保护

- 所有淘宝、知乎和转链请求必须经过 Edge Functions，前端不得直连私密接口；
- 按 Supabase 用户 ID、脱敏 IP 哈希、接口类型和时间窗口组合限速，不以 IP 作为用户身份；
- 相同规范化商品查询在短时间内优先使用服务端缓存，缓存只保存商品与公开证据结果，不缓存用户身份或私密数据；
- 设置单用户日限额、全站日预算和并发上限；接近知乎或淘宝额度时优先保护已创建愿望和到期决策，暂停新的探索性搜索；
- 匿名注册、邮箱 OTP 和外部搜索分别限速，不能共用一个粗粒度阈值；
- 正常用户不显示 CAPTCHA；只有达到异常频率、自动化特征或全局攻击阈值时，才要求完成人机验证；
- 超过限额时返回明确、诚实的稍后重试提示，不用固定商品或伪造实时数据顶替；
- 日志记录脱敏的限速原因、请求量和缓存命中，不记录完整邮箱、搜索隐私内容或外部密钥。

---

## 12. 外部接口使用规范

### 12.1 淘宝联盟搜索

- 只使用 `taobao.tbk.dg.material.optional.upgrade`；
- 使用权限包 `27939`；
- 传入由完整 PID 派生的 `site_id` 和 `adzone_id`；
- 商品搜索默认请求 5 件；
- 响应统一映射为 `ProductCard`；
- 不把淘宝原始错误结构直接暴露给浏览器；
- 不调用已验证失败的旧接口作为自动重试。

### 12.2 知乎搜索

- 每个愿望最多两次主搜索；
- 每次默认请求 5 条；
- 两次搜索并行发起；
- 仅保留展示所需字段；
- 搜索摘要不冒充全文；
- 原文链接必须可点击；
- 超出额度、认证失败、无结果和服务端错误分别处理。

### 12.3 淘宝联盟官方推广链接

- 商品搜索只接受升级版物料响应中带有营销 `item_id`、标题、价格及有效 `promotionUrl` 的候选；
- `promotionUrl` 的取值顺序固定为 `item.publish_info.coupon_share_url`、`item.publish_info.click_url`；
- 以 `//` 开头的官方链接规范化为 `https://`；不拼接、改写或伪造；
- 用户选择“还想买”时只使用创建愿望时保存的该值，不二次转链；
- 淘客转链 Skill 源码保留用于独立历史诊断，但不在 Demo 主路径调用，也不作为降级或自动重试手段。

---

## 13. 异常与降级策略

### 13.1 淘联搜索失败

| 场景 | 用户提示 | 操作 |
|---|---|---|
| 网络超时 | 暂时没找到商品，请重试 | 原查询重试一次 |
| 无结果 | 换个更具体或更简单的商品名试试 | 返回输入页 |
| 权限失败 | 商品服务暂时不可用 | 停止重试并记录脱敏错误 |
| 部分字段缺失 | 过滤无核心字段结果 | 候选不足时明确提示 |

禁止自动切换到旧版 `16516/16518` 接口。

### 13.2 知乎搜索失败

- 一层失败不阻塞另一层展示；
- 两层失败仍允许用户保管商品；
- 明确告诉用户本次资料不足；
- 不用固定演示内容替换实时失败结果；
- 提供“重新搜索知乎内容”入口。

### 13.3 官方推广链接缺失或落地失败

1. 搜索阶段过滤没有有效官方推广链接的记录；
2. 已保存链接无法落地时，保留愿望并提示稍后重试；
3. 不拼接链接、不伪造链接、不自动调用转链 Skill。

不允许将无效或空链接绑定到购买按钮。

### 13.4 身份、愿望存储或跨浏览器登录失败

- 服务端未确认保存成功前，不展示“愿望已收好”；
- 保存失败时保留当前页面数据，允许重试，不静默假装成功；
- 匿名登录失败时不允许创建只存在浏览器内的假愿望；提示服务暂时不可用并允许重试；
- 已绑定邮箱用户会话过期时，允许重新发送邮箱验证码登录；未绑定匿名用户会话丢失时如实说明无法识别原身份；
- 验证码错误、过期、邮件延迟分别给出可操作提示；错误响应不透露该邮箱是否存在；
- 验证码发送与校验连续失败触发限速；
- 匿名愿望迁移失败时保持源愿望不变，允许幂等重试，不出现愿望消失或金额重复；
- 微信与知乎 WebView 的本地会话不同不算数据丢失；同一邮箱验证登录后应读取同一服务端愿望；
- 邮件服务不可用时，匿名主流程仍可使用，但明确提示跨浏览器同步暂不可用。

### 13.5 页面刷新或关闭

- 根据 `expiresAt` 重新计算剩余时间；
- 优先从服务端读取当前稳定状态，已到期则恢复到 `expired`；
- 已完成结局不得重新累计金额；
- 正在调用外部接口的临时加载状态不持久化，刷新后回到最近稳定状态。

---

## 14. 安全与隐私要求

### 14.1 必须满足

1. 所有密钥只在服务端；
2. `.env` 和本地凭据不进入 Git；
3. 凭据文件权限应为 `600`；
4. 日志只保留请求阶段、耗时、结果数量和脱敏错误；
5. 外部内容用 `textContent` 或等价安全渲染；
6. 外部 URL 只允许 `https`；
7. 新窗口链接使用安全的 `rel` 属性；
8. 输入长度受限；
9. 后端校验所有请求字段，不相信前端传入的价格和 PID；
10. 所有愿望表启用并强制 RLS，使用 `auth.uid() = owner_id` 限制读取、写入、更新和删除；
11. Supabase secret key、淘宝、知乎和转链密钥仅在 Edge Functions Secrets 中，绝不进入 GitHub 仓库或 Pages 构建产物；
12. Edge Functions 验证用户 JWT、允许来源、输入字段、限速和幂等键；不得信任前端提供的 `ownerId`、价格、PID 或最终状态；
13. 匿名愿望迁移必须同时验证两个会话，在短事务中完成；不得在持锁事务中调用外部接口；
14. 邮箱验证码仅用于身份验证，短时有效、限制尝试次数；日志和监控中的邮箱必须脱敏或哈希；
15. 删除单条愿望、清空记录和删除账户使用不同确认文案；删除账户后撤销会话并清理关联业务数据；
16. 不使用浏览器指纹、IP 地址、设备特征拼接或第三方 Cookie 追踪用户；IP 只允许短期用于安全限速，不作为身份；
17. 不采集姓名、手机号、地址或支付信息；邮箱不用于营销。

### 14.2 数据保留与用户控制

服务端只保存完成愿望流程必要的数据：

- 商品展示数据；
- 知乎证据摘要；
- 保管时间和状态；
- 用户最终选择；
- 愿望金额快照；
- Supabase Auth 用户 ID；
- 邮箱身份由 Supabase Auth 保存，业务表不复制邮箱。

Supabase SDK 可以在浏览器保存标准登录会话；`localStorage` 仍不得保存愿望正文、价格统计、邮箱或自制认证凭证，只允许 Auth SDK 会话和非敏感界面偏好。页面提供“删除愿望”“清空愿望”“退出当前设备”和“删除账户”，并清楚区分：退出或清除浏览器数据不会删除服务器愿望，已绑定邮箱可重新登录；未绑定匿名用户可能无法再识别原身份。

### 14.3 隐私告知

- 在首次收集邮箱前，以简短、清晰文案说明处理者、用途、保存期限、服务提供方和删除方式；
- 邮箱绑定必须由用户主动发起，不得默认勾选营销、活动通知或其他无关用途；
- 提供可随时访问的隐私说明，列明 Supabase 作为身份与数据服务提供方；
- 明确匿名使用不要求邮箱；用户不同意提供邮箱时，仍可在当前会话环境中使用核心 Demo；
- 保留期到期或用户删除账户后，按既定规则删除或匿名化相关数据；
- 上线前根据实际部署区域、服务条款和活动主体完成一次隐私与跨境数据合规核对。本规格只定义产品最小化原则，不代替正式法律意见。

---

## 15. 非功能要求

### 15.1 兼容性

重点支持：

- 微信 App 内置浏览器：iOS 与 Android；
- 知乎 App 内置浏览器：iOS 与 Android；
- iOS Safari 和 Android Chrome，作为内置浏览器问题的外部浏览器降级环境；
- 桌面 Chrome，作为开发和演示控制环境；
- 手机宽度重点覆盖 320、360、375、390、393、414、430 px；
- 桌面宽度 1280 px 及以上。

### 15.2 微信与知乎 WebView 专项适配

以下要求是正式功能范围，不是发布后的“有空再优化”：

1. 使用 `viewport-fit=cover` 和 `env(safe-area-inset-*)` 处理刘海、灵动岛和底部 Home Indicator；
2. 页面高度使用 `dvh` 等动态视口能力，并提供兼容回退，不能只依赖 `100vh`；
3. 输入框获得焦点、软键盘弹出时，主要按钮和候选列表不能被遮挡；
4. 底部固定操作区必须叠加安全区，不与内置浏览器工具栏冲突；
5. 监听 `visibilitychange` 和 `pageshow`，从后台返回时重新按服务端 `expiresAt` 计算倒计时；
6. 不依赖第三方 Cookie、弹窗窗口或自动唤端完成主流程；邮箱登录优先使用当前页输入一次性验证码，减少跨 App 跳转失败；
7. 所有外跳必须由用户明确点击触发，禁止自动跳转或模仿微信/知乎原生 UI；
8. 淘宝 CPS 短链需分别在微信和知乎内置浏览器实测；若被拦截或无法正确唤起，则显示降级页，提供“复制淘口令/链接”和“在外部浏览器打开”的清晰说明；
9. 知乎原文链接在知乎 WebView 中优先正常打开，在微信 WebView 中允许按平台规则继续访问或复制；
10. 页面不能依赖 UA 字符串决定核心业务，只将 WebView 检测用于提示和降级；
11. iOS 与 Android 的分享、返回手势、页面恢复均需单独验证；
12. 邮箱验证码发送、切到邮箱 App、返回 WebView 和重新加载后的页面状态均需实测；不能假设 WebView 一直驻留；
13. 不在首版接入未经确认的微信 JS-SDK 或知乎私有 JSBridge。若官方活动提供专用桥接能力，另行补充规格和验收。

### 15.3 手机布局基线

- 单列布局优先，内容最大宽度在大屏手机上仍保持易读；
- 正文最小可读字号原则上不低于 14 px，主要操作不低于 16 px；
- 点击目标建议不小于 44×44 CSS px；
- 商品卡在 320 px 宽度下不产生横向滚动；
- 长商品标题最多显示合理行数，并允许查看完整标题；
- 价格、按钮和倒计时在系统字体放大后仍不重叠；
- 横屏不是主体验，但不得白屏或无法操作；
- 禁止页面整体意外缩放和水平抖动。

### 15.4 性能与等待体验

- 首屏不依赖外部接口即可显示；
- 商品搜索和知乎搜索分别设置明确超时；保留的转链适配器不属于 Demo 请求链路；
- 搜索期间展示阶段性进度；
- 图片使用懒加载和合理尺寸；
- 不因一张外部图片失败阻塞整页。

### 15.5 可访问性

- 所有按钮可用键盘触发；
- 输入框有可见标签；
- 倒计时不只依赖颜色表达；
- 动画遵守 `prefers-reduced-motion`；
- 关键文字保证足够对比度；
- 商品图片提供替代文本。

### 15.6 可维护性

- 淘宝和知乎分别独立适配；转链适配器与 Demo 主链路隔离；
- 页面不直接解析外部平台原始响应；
- 状态迁移集中管理；
- 金额计算集中管理；
- 不在多个页面复制相同业务判断；
- 关键外部调用保留可独立运行的冒烟验证。

---

## 16. 测试方案

### 16.1 静态检查

- JavaScript/TypeScript 语法检查；
- 格式检查；
- 未使用变量和明显错误；
- 构建成功；
- 构建产物中搜索密钥特征，确保无泄露。

### 16.2 单元测试

至少覆盖：

- 商品响应字段归一化；
- 价格和券后价转换；
- 商品标题清洗；
- 知乎两层内容筛选；
- 摘要安全截断；
- 倒计时剩余时间计算；
- 匿名用户创建和刷新后会话恢复；
- 邮箱身份绑定后 `ownerId` 保持不变；
- 新浏览器邮箱 OTP 登录、过期和错误次数限制；
- 匿名愿望向既有邮箱账户迁移的原子性、幂等和去重；
- RLS 阻止跨用户读取、更新和删除；
- 清除业务界面缓存不丢失服务端愿望；
- 放弃金额只累计一次；
- 状态机非法跳转保护；
- 官方推广链接的过滤、规范化与落地失败边界。

### 16.3 接口集成测试

#### 淘联

- 常见品类：便携咖啡机；
- 标准型号：明确品牌和型号；
- 长尾商品；
- 无结果关键词；
- 特殊字符；
- 超时和权限错误。

每次确认返回的商品标题、图片、价格和营销 ID 能形成标准卡片。

#### 知乎

- 内容充足商品；
- 只有专业内容；
- 只有体验内容；
- 完全无结果；
- 额度或凭证错误；
- 摘要包含 HTML 高亮标签。

#### 官方推广链接

- `coupon_share_url` 优先、`click_url` 后备；
- `//` 前缀规范化；
- 缺少有效链接的记录被过滤；
- 当前标签页打开链接后商品一致。

### 16.4 浏览器端端到端测试

必须覆盖：

1. 正常搜索、选择、双层卡、24 秒倒计时、购买；
2. 正常搜索、选择、双层卡、24 秒倒计时、放弃；
3. 48 秒和 72 秒选项；
4. 倒计时中刷新页面；
5. 到期页刷新；
6. 重复点击最终选择；
7. 商品无结果；
8. 知乎部分结果；
9. 官方推广链接缺失时进入明确错误状态，不进行二次转链；
10. 320、360、375、390、414、430 px 手机尺寸完整流程；
11. 微信 iOS/Android 内置浏览器完整流程；
12. 知乎 iOS/Android 内置浏览器完整流程；
13. Safari 和 Chrome 完整流程；
14. 软键盘弹出、切后台再返回、前进后退；
15. 未绑定邮箱时完成完整匿名主流程，正常流程中不出现强制注册；
16. 完成首个愿望后只出现一次非阻断邮箱绑定提示；
17. 原匿名用户绑定邮箱后既有愿望完整保留；
18. 清除全部网站数据后，已绑定用户通过邮箱 OTP 找回完整时间线；
19. 新浏览器先创建匿名愿望，再登录既有邮箱账户，愿望安全迁入且不重复金额；
20. 隐私模式匿名完成主流程，关闭后不虚假承诺找回；绑定邮箱后可重新登录；
21. 微信、知乎、Safari、Chrome 使用同一邮箱 OTP 登录后显示同一愿望集合；
22. 邮箱验证码延迟、错误、过期、限速与重发；
23. 从 WebView 切至邮箱再返回，输入和页面状态可继续或正确恢复；
24. RLS 越权读写测试；
25. 淘宝链接在两个 WebView 中的正常外跳和拦截降级；
26. 删除单条愿望、清空愿望、退出当前设备和删除账户。

### 16.5 演示排练

- 使用一个提前确认内容充足的商品进行主流程排练；
- 这只是演示排练用例，不是产品固定商品；
- 现场仍允许测试者输入任意商品；
- 排练至少完成三次连续成功；
- 演示设备关闭无关通知和插件干扰；
- 提前通过微信和知乎中的实际入口验证现场网络与部署地址；
- 准备接口失败时的诚实说明，不展示伪造实时结果。

---

## 17. 分阶段开发流程

每个阶段完成后单独报告“代码完成、静态检查、接口验证、浏览器验证”四种状态，不能混为一句“已完成”。

### 阶段 0：规格确认

产出：

- 本文档；
- 最终确认的待决项；
- 明确的开发范围冻结。

退出条件：用户明确批准本文档进入开发。

### 阶段 1：部署与身份持久化开工验证

工作：

- 创建 GitHub Pages 最小前端和 Supabase 项目；
- 验证 Pages 到 Edge Functions 的 HTTPS、CORS 和目标 WebView 网络；
- 完成匿名登录、写入一条测试愿望、刷新和关闭后重开；
- 绑定邮箱，在另一浏览器通过 OTP 登录并读取同一愿望；
- 重新部署 GitHub Pages 和 Edge Functions，确认用户与愿望数据仍存在；
- 验证 RLS 拒绝另一个用户读取该愿望；
- 验证淘宝、知乎请求能从 Edge Functions 发出，且密钥不出现在前端；验证官方推广链接在当前标签页的受限落地；
- 验证正式邮件服务能向非项目成员邮箱送达验证码。

状态：**已完成**。验收证据见 `PHASE1_ACCEPTANCE.md`；阶段 1 诊断页保留为 `diagnostic.html`。

### 阶段 2：项目骨架与静态流程

工作：

- 建立正式产品页与阶段 1 诊断页隔离的最小前端目录；
- 完成基础视觉、响应式布局和页面状态机；
- 使用本地 fixture 走通全部页面；
- 复用已完成的阶段 1 Auth、愿望表和 RLS 基线，但本阶段不调用或修改它们；
- 本地 `sessionStorage` 仅用于开发 fixture 刷新恢复，不是正式持久化方案。

注意：fixture 必须标记为开发测试数据，不能在最终产品中冒充实时结果。

状态：**已完成**。验收证据见 `PHASE2_ACCEPTANCE.md`；冻结代码基线为 `4c29383`。退出条件已满足：不依赖外部接口也能完成页面和状态验证，且生产构建不把 fixture 伪装成真实在线结果。

### 阶段 3：淘联真实商品搜索

工作：

- 封装签名和 `27939` 升级版请求；
- 统一映射 `ProductCard`；
- 接入商品候选页面；
- 完成无结果、超时和权限错误处理。

退出条件：任意测试关键词可返回真实候选，页面不泄露密钥。

状态：**实施完成，等待独立验收**。本地实现、自动化测试与浏览器边界证据见 `PHASE3_ACCEPTANCE.md`；真实函数部署和真实接口验收尚因本机 Supabase CLI 缺失而阻塞，不能提前宣称退出条件已满足。

### 阶段 4：知乎双层内容

工作：

- 封装两路知乎搜索；
- 商品标题清洗；
- 证据筛选和安全摘要；
- 双层卡及来源链接；
- 部分结果和无结果降级。

退出条件：至少三类不同商品完成实时双层搜索验证。

### 阶段 5：愿望保管、服务端持久化与跨浏览器身份

工作：

- 24/48/72 秒模拟；
- 封存动画；
- Supabase 匿名登录；
- “我的愿望”时间线与刷新、关闭后自动回溯；
- 可选邮箱绑定和 OTP 登录，不提供愿望码或密码；
- 匿名愿望迁移到既有邮箱账户；
- Postgres RLS、索引、保留期和删除；
- 清除网站数据、隐私模式以及微信/知乎 WebView 会话边界；
- 两个结局；
- 服务端记录和金额统计；
- 防重复累计。

退出条件：刷新、返回、重复点击等边界全部通过。

### 阶段 6：官方推广链接与购物卡

前置条件：已完成。

工作：

- 使用创建愿望时保存的官方 `promotionUrl`；
- 不二次转链；
- 只为有有效官方推广链接的候选展示外跳；
- 购买卡和外跳。

退出条件：商品一致、链接有效、无效链接不会进入候选。

### 阶段 7：视觉完善与浏览器验证

工作：

- 动画和看山人格细节；
- 手机尺寸、微信 WebView、知乎 WebView、外部浏览器和桌面验证；
- 安全区、动态视口、软键盘、前后台切换和外链降级；
- 可访问性；
- 加载与错误状态；
- 控制台错误清零。

退出条件：主流程在目标浏览器连续通过。

### 阶段 8：部署与最终演示验收

工作：

- 配置部署环境密钥；
- 公网地址验证；
- 实时接口回归；
- 三次完整演示排练；
- 最终安全检查。

退出条件：满足本文档“完成定义”。

---

## 18. 完成定义（Definition of Done）

只有同时满足以下条件，Demo 才能称为完成：

### 功能

- [ ] 任意合法商品关键词可以提交；
- [ ] 淘联返回真实候选；
- [ ] 用户可以选择具体商品；
- [ ] 知乎双层内容实时生成并可溯源；
- [ ] 三种演示时长可选；
- [ ] 首次使用无需注册，刷新、关闭后重开可自动进入当前匿名用户的愿望保管箱；
- [ ] 用户可自愿绑定邮箱；绑定后可在不同浏览器和 WebView 通过邮箱 OTP 查看同一愿望时间线；
- [ ] 匿名用户升级邮箱身份和匿名愿望迁移均不丢失、不重复愿望或金额；
- [ ] 清除浏览器数据和隐私模式的限制有准确提示；正常流程不展示愿望码或强制登录；
- [ ] 购买和放弃两个结局均可完成；
- [ ] 购物链接有效；
- [ ] 放弃金额不会重复累计；
- [ ] “我的愿望”时间线可以查看；愿望删除、退出当前设备和删除账户边界明确。

### 可靠性

- [ ] 淘联、知乎及官方推广链接缺失/落地失败均有明确错误状态；
- [ ] 无结果不会展示假数据；
- [ ] 外部图片失败不阻塞流程；
- [ ] 连续三次完整演示成功；
- [ ] 重点手机尺寸和桌面通过；
- [ ] 微信与知乎 iOS/Android 内置浏览器通过；
- [ ] Safari 和 Chrome 降级环境通过；
- [ ] 淘宝外链在两个内置浏览器中可用或正确降级；
- [ ] 邮箱验证码在目标浏览器中可送达，跨 App 返回后流程可继续；
- [ ] GitHub Pages 重新部署后，Supabase 中的用户和愿望数据仍然存在；
- [ ] 公开地址的单用户限速、全站预算、查询缓存和异常流量熔断有效，正常用户不被强制 CAPTCHA；
- [ ] 控制台无未处理异常。

### 安全

- [ ] 前端仅含公开设计的 Supabase URL 与 publishable key；所有私密密钥均不存在于前端和构建产物；
- [ ] `.env` 和凭据被 Git 忽略；
- [ ] 本地凭据权限符合要求；
- [ ] 日志不输出敏感值；
- [ ] 外部内容安全渲染；
- [ ] 所有愿望表启用并验证 RLS，用户无法读取或修改他人的愿望；
- [ ] Supabase secret key 和外部平台密钥不进入 GitHub Pages 构建产物；
- [ ] 不采集支付数据、姓名、手机号或地址；邮箱仅用于用户主动选择的身份验证，不用于营销。

### 交付

- [ ] 本地启动说明完整；
- [ ] 环境变量清单完整但不含真实值；
- [ ] 测试命令可运行；
- [ ] 部署地址可访问；
- [ ] 已区分代码完成、静态检查、接口验证和浏览器验证状态。

---

## 19. 已冻结的产品与技术决策

以下内容是本轮开发基线，不再作为开发过程中的临时选择：

| 决策项 | 已确认结论 |
|---|---|
| 前端技术 | Vite Vanilla |
| 部署目标 | GitHub 管理源码；GitHub Pages 托管前端，Supabase 托管 Auth/Postgres/Edge Functions |
| 商品候选数 | 5 件 |
| 知乎内容数 | 每层最多 3 条 |
| 时长 | 保留 24/48/72 三档，按秒模拟 |
| 历史记录 | “我的愿望”独立视图或全屏面板，支持多个愿望时间线 |
| 数据持久化 | Supabase Auth 用户＋Postgres；匿名先用，邮箱 OTP 自愿绑定和跨浏览器登录 |
| 数据保留 | 匿名用户按最后愿望操作保留 30 天；已绑定用户保留至主动删除，整体下线须提前公示 |
| 邮件验证 | 当前页输入邮箱 OTP，不依赖魔法链接跳转；正式演示前配置真实可送达邮件服务 |
| 多愿望 | 同时最多 5 个未完成愿望，各自独立倒计时 |
| WebView | 微信与知乎 iOS/Android 均列为必测环境 |
| 外链降级 | 复制淘口令/链接＋外部浏览器提示 |
| 购买链接 | 淘联升级版物料搜索返回的官方 `promotionUrl`；无有效链接的商品不进入候选 |
| 金额 | `finalPrice` 优先，`price` 兜底 |
| 图片入口 | 首页保留禁用占位，明确“暂未开放” |
| 内容不足 | 允许继续保管并明确告知 |
| 年度账单 | 只做概念彩蛋 |

### 19.1 开发前仍需完成的外部配置

这些不是未决产品方案，而是阶段 1 必须实际完成并验收的环境工作：

1. 创建 Supabase 项目并选择部署区域；
2. 配置可向任意测试邮箱送达 OTP 的事务邮件服务、发信域名和发件人；
3. 确定并冻结 GitHub Pages 正式地址；如使用自有域名，完成域名验证和 HTTPS；
4. 把淘宝、知乎、转链和 Supabase 私密值配置为 Edge Functions Secrets；
5. 开启匿名登录、邮箱身份绑定、RLS、限速和异常流量保护；
6. 配置匿名用户每日清理任务；
7. 完成隐私说明中的处理主体、联系方式、Supabase 服务提供方、保存期限和删除入口；
8. 核对 GitHub Pages 对本活动 Demo 与淘客外跳的适用性。

任何一项未完成，都不能宣称公网 Demo 已具备跨浏览器持久化与真实用户使用条件。

### 19.2 GitHub Pages 退出条件

源码持续托管于 GitHub；默认先使用 GitHub Pages。出现下列任一情况时，只迁移前端静态托管，不修改 Supabase 后端和产品架构：

- GitHub Pages 使用规则明确不允许当前活动 Demo 或淘客外跳；
- 微信或知乎 WebView 无法稳定访问 Pages 域名；
- Pages 项目子路径导致登录、刷新或资源路径问题无法可靠解决；
- 正式域名、HTTPS 或跨域配置无法满足安全要求。

迁移前端托管属于既定降级方案，不算产品范围扩大；实际迁移仍需在执行前向用户报告。

---

## 20. 变更记录

| 版本 | 日期 | 说明 |
|---|---|---|
| `v0.1` | 2026-08-11 | 根据产品规划、知乎开放平台和淘联实测结果形成首版完整规格 |
| `v0.2` | 2026-08-12 | 确认转链与凭证安全验收；改为服务端愿望持久化和无登录恢复；加入微信、知乎 WebView 与手机专项适配 |
| `v0.3` | 2026-08-12 | 将恢复码方案改为无感匿名愿望保管箱：服务端持久化、安全 Cookie 自动回溯、“我的愿望”时间线；一次性恢复链接降为可选备份，并明确 WebView、清站点数据和隐私模式边界 |
| `v0.4` | 2026-08-12 | 固定 GitHub Pages 前端＋Supabase 后端；引入匿名 Auth、可选邮箱 OTP、跨浏览器同步、RLS、匿名愿望迁移和多愿望并行规则，移除恢复链接方案 |
| `v1.0` | 2026-08-12 | 用户确认 v0.4 方向；冻结产品与技术基线，补齐数据保留和匿名清理、邮件与隐私配置、GitHub Pages 退出条件及阶段 1 开工闸门；仍未启动业务开发 |
| `v1.1` | 2026-08-15 | 记录阶段 1 完成；阶段 2 开始本地静态产品流程。正式路径明确直接使用物料搜索返回的官方推广链接，不再二次转链；移除陈旧 `link_converting` 与 `/api/links/convert` 正式业务路径。 |
| `v1.2` | 2026-08-15 | 阶段 2 静态产品流程完成、异步导航缺陷修复、验收冻结；代码基线为 `4c29383`，阶段 3 未开始。 |
| `v1.3` | 2026-08-15 | 阶段 3 淘联真实商品搜索本地实现完成，等待独立验收；记录真实函数部署与接口验收的 CLI 阻塞，阶段 4 未开始。 |
